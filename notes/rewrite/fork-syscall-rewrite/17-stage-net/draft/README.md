# 17-stage-net — 网络（占位）

> **状态**: 占位（crate 已建，非目标可长期后置）
> **定位**: `minix3/minix/net/` 网络服务

## 范围
- `os/net/lwip`（TCP/IP 栈 server）
- `os/net/uds`（Unix domain socket server）
- 框架: `os/libs/minix-netdriver`

## C 对应
- `minix3/minix/net/`

## 说明
- 与"完整启动 + sample 游戏"目标无关，占位后置
