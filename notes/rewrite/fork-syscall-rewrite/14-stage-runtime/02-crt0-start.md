# 02 程序入口：从第一条指令到 main 函数调用

> **目标读者**：已经读过第 01 篇的读者，理解内核信息页面和初始栈的概念。前置知识只需要一样：知道函数调用需要把参数放在约定的寄存器或者栈位置，知道链接器负责把多个目标文件拼成一个可执行文件。
> **本文在进程运行时生命周期中的位置**：诞生阶段的第二步。内核已经把页面指针和栈顶准备好，程序的第一条指令开始执行。本文回答从第一条指令到 `main` 函数被调用之间，每一步都在做什么：汇编桩如何整理栈和寄存器，C 入口函数如何发布环境变量、推导程序短名、运行启动函数列表，以及为什么静态链接的程序永远走不到动态加载器的分支。
> **前置依赖**：第 01 篇 `01-kernel-handoff.md`（内核信息页面与初始栈），以及 `00-runtime-overview.md` 对生命周期主线的介绍。
> **本篇不覆盖的内容**：内核信息页面的查询与校验细节（那是第 01 篇的主题），通信向量表的安装过程（那是第 03 篇的主题），内存分配器的实现（那是第 06 篇的主题），进程退出消息的语义（那是第 08 篇进程管理分组的主题）。

---

## 1 概念：为什么 main 函数不能是第一条指令

初学者常常以为程序是从 `main` 函数的第一行开始执行的。实际上在 `main` 的第一行运行之前，有一大堆准备工作必须完成：环境变量指针要发布成全局变量，程序短名要从完整路径里截出来，C 语言库的内部状态要初始化一遍，编译器和链接器收集的启动函数要按顺序运行一遍，退出时要运行的关闭函数要提前登记好。这些工作不属于任何一个应用程序员的业务逻辑，但是每个程序都需要它们，所以它们被集中放在一个叫做 C 语言运行时启动文件的部件里，传统上简称为启动文件。

启动文件分成两层。最外层是几行汇编代码，它的名字在不同系统里略有差异，在 Minix 的 64 位 Intel 版本里叫做 `__start`，定义在 `minix3/lib/csu/arch/x86_64/crt0.S` 文件里。汇编层的工作非常具体：把栈指针调整到 16 字节对齐，预留 8 字节的影子空间，把三个入口参数搬运到 C 语言调用约定的寄存器里，然后跳转到 C 入口函数。里层是 C 语言函数 `___start`，定义在 `minix3/lib/csu/common/crt0-common.c` 文件的第 144 行到第 192 行，它完成上面列出的全部准备工作，最后调用 `main` 并把返回值交给 `exit`。

这里有一个贯穿全文的关键区别：Linux 和 Redox 把参数个数、参数指针、环境指针直接放在初始栈上，入口汇编从栈里把它们挖出来；而 Minix 把这三样东西打包成一个进程字符串描述结构，只传递一个指向该结构的指针，另外再带两个加载器相关的值。这种差异不是偶然的，它来自不同的历史源头：Minix 的启动文件继承自 NetBSD，而 NetBSD 的进程状态查看工具要求描述结构常驻栈顶，启动文件顺手复用了这个结构。理解了这个源头，就不会对 Minix 为什么多一层间接感到奇怪。

本章剩下的篇幅先建立三个心智模型：入口参数的三元组模型，启动函数列表的有序运行模型，以及静态链接下动态分支消失的模型。下一章再逐行对照 C 语言源码验证这些模型。

### 1.1 入口参数的三元组模型

程序入口收到的不是参数个数和指针数组，而是三个值：清理回调函数、加载器对象描述指针、进程字符串描述结构指针。前两个值只在动态链接的程序里有意义，静态链接的程序里它们永远是空的。第三个值永远有效，指向的结构体里装着参数个数、参数指针、环境指针、环境个数。C 入口函数的第一件事就是检查第三个值是不是空指针，空指针意味着内核或者加载器没有正确布置启动信息，程序没有任何依据可以继续运行，只能立刻退出。

### 1.2 启动函数列表的有序运行模型

编译器允许程序员用特殊标记把函数登记到启动列表里，链接器把这些函数指针收集成连续的数组。Minix 的启动文件处理三个列表：预初始化列表、初始化列表、结束列表。预初始化列表和初始化列表在 `main` 之前按从前到后的顺序运行，结束列表通过 `atexit` 登记，在 `exit` 的时候运行。此外还有一套更老的构造器列表，为了兼容不支持新式数组的老工具链，运行时按从后到前的逆序运行。顺序在这里就是语义：依赖关系靠登记顺序表达，登记在前的先运行。

### 1.3 静态链接下动态分支消失的模型

C 入口函数里有一段检查动态链接器的代码：查看弱引用符号 `_DYNAMIC` 是否为空，再检查加载器对象描述的魔数和版本号。静态链接的程序没有动态段，弱引用符号的值永远是空，整个分支永远不会进入。Rust 实现里干脆不为这个分支建模，因为本运行时只支持静态链接（阶段计划里的架构演进第一项）。保留一个永远走不到的分支只会误导读者，让读者以为动态链接是本运行时的能力之一。

---

## 2 C 语言源码分析：入口路径上的每个文件在做什么

本章按执行顺序讲解入口路径上的全部 C 语言文件和函数。所有行号都针对当前代码库中的 Minix3 源码。

### 2.1 汇编桩：7 行指令完成的三件事

打开 `minix3/lib/csu/arch/x86_64/crt0.S` 文件的第 43 行到第 49 行：

```
_ENTRY(__start)
    andq    $~15,%rsp
    subq    $8,%rsp
    movq    %rdx, %rdi
    movq    %rcx, %rsi
    movq    %rbx, %rdx
    jmp     ___start
```

第一行是入口符号定义，同时文件第 40 行用别名机制把 `_start` 指向 `__start`，所以链接器无论找哪个名字都能找到入口。第二行的按位与操作把栈指针向下 rounding 到 16 字节的整数倍，这是 64 位 Intel 架构的调用约定要求的：每次执行调用指令的时候，栈指针在减去返回地址之前必须是 16 字节对齐的，函数内部才能安全地使用需要对齐的向量指令。第三行再减去 8 字节，是为了给调用指令即将压入的返回地址预留位置，使得跳转到 C 函数之后的栈依然满足对齐要求。最后三行搬运参数：内核或者加载器把三个入口值放在 `rdx`、`rcx`、`rbx` 寄存器里，而 C 语言调用约定要求前三个参数放在 `rdi`、`rsi`、`rdx` 寄存器里，所以需要三次搬运。最后一行是跳转而不是调用，意味着汇编桩不会再拿回控制权，C 入口函数直接继承了整理好的栈。

读者可能会问：为什么不用调用指令而用跳转指令？因为入口桩不需要返回。调用指令会压入返回地址，而这里根本没有地方可以返回——入口桩是整个进程的第一段代码，它的调用者是内核，内核不会回来取返回值。用跳转指令可以少压一次栈，保持栈的干净。

### 2.2 全局变量：环境指针与程序短名的初始值

打开 `minix3/lib/csu/common/crt0-common.c` 文件的第 77 行到第 81 行：

```c
char       **environ;
struct ps_strings *__ps_strings = 0;
static char     empty_string[] = "";
char        *__progname = empty_string;
```

`environ` 是指向环境指针数组的全局变量，初始值没有显式给出，取决于它所在的编译单元。Minix 还有另一个编译单元 `minix3/minix/lib/libc/sys/environ.c`，它把 `environ` 定义成一个特殊的哨兵值 `0x53535353`，注释里解释了选择这个值的理由：只比较低两个字节，因为不同机器的指针宽度和字节序不一样，所以四个字节都填同样的图案，保证在任何机器上比较结果都一致。启动文件的职责之一就是用描述结构里的真实环境指针覆盖掉这个哨兵值。

`__ps_strings` 是指向进程字符串描述结构的全局指针，初始值是空，入口函数会把它设置成真实地址。`__progname` 是程序短名，初始值指向空字符串，入口函数会把它设置成从第一个参数里截出来的短名。这三个全局变量的共同特点是：它们在入口函数运行之前处于哨兵或者空的状态，入口函数运行之后处于真实状态，后续的库函数和应用程序都可以直接读取它们。

### 2.3 入口函数的完整流程

同一个文件的第 144 行到第 192 行是 `___start` 函数的完整实现。我们按执行顺序逐段讲解。

第 150 行到第 152 行检查描述结构指针是不是空指针。空指针意味着启动信息丢失，函数通过 `_FATAL` 宏输出一行错误信息并退出进程。在 Minix 上 `_FATAL` 宏里的写操作是被禁用掉的（第 89 行把写操作定义成空），所以实际效果是直接退出，退出码为 1。这个细节说明启动早期的报错通道是不可靠的，程序能做的就是尽快停下来。

第 154 行发布环境指针：第 152 行保存描述结构指针到全局变量之后，第 154 行把描述结构里的环境指针复制到全局 `environ` 变量。从这一行开始，标准库里的环境变量查询函数才能正常工作。

第 156 行到第 165 行推导程序短名。代码先把 `__progname` 指向第一个参数的完整路径，然后逐个字符扫描，每遇到一个斜杠就把指针移到斜杠之后，最终停在最后一个斜杠之后的位置。举例来说，完整路径 `/usr/bin/hello` 经过这个循环之后，指针指向 `hello` 的开头。如果参数列表是空的（第一个参数就是空指针），代码保持初始的空字符串。这个循环用字节比较而不是字符解码，所以路径里即使有非文本编码的字节也不会出错，行为和 C 语言的字符串函数完全一致。

第 167 行到第 175 行是动态链接器检查。`rtld_DYNAMIC` 是一个弱引用符号，静态链接的程序里它的地址是零，条件不成立，整个分支被跳过。动态链接的程序里它非零，代码继续检查加载器对象描述的魔数和版本号，任何一项对不上就报错退出，通过之后用 `atexit` 登记加载器提供的清理回调。这个分支是本运行时明确不支持的部分，Rust 实现里没有对应的阶段，文档里保留这一节是为了让读者明白静态链接的程序为什么永远不会走到这里。

第 177 行调用 C 语言库初始化函数 `_libc_init`。这个函数本身在 `minix3/lib/libc/misc/initfini.c` 文件的第 84 行到第 119 行，它用一个静态标志保证只运行一次，依次完成辅助向量定位、栈保护初始化、线程原子操作初始化、线程本地存储初始化、线程库初始化、退出函数互斥锁初始化、环境变量红黑树初始化。其中的线程相关步骤只在多线程构建里编译，单线程的静态程序会跳过它们。

第 179 行到第 181 行运行预初始化列表，第 188 行到第 189 行登记结束列表并运行初始化列表。列表的定义在第 106 行到第 117 行，全部声明成弱引用符号，所以没有任何登记函数的程序也能正常链接，空列表的起始和结束指针相等，循环体一次也不执行。第 191 行调用 `main` 函数并把返回值交给 `exit`。从这一行开始，控制权正式移交给应用程序员的代码，启动文件的使命结束。

### 2.4 构造器兼容层的逆序运行

打开 `minix3/lib/csu/common/crtbegin.c` 文件的第 72 行到第 97 行，可以看到老式构造器列表的运行函数。它用一个静态标志保证只运行一次，注册异常帧信息，注册 Java 类（在 Minix 上永远是空操作），然后从列表尾部向头部逆序调用每个函数。逆序的原因是历史遗留的：老式列表在目标文件里是按逆序存放的，链接器拼接之后需要倒着运行才能恢复登记顺序。新式数组没有这个问题，所以按正序运行。Rust 实现用一个方向枚举统一了这两种顺序，调用者只需要说明是正序还是逆序，不需要知道背后的历史原因。

---

## 3 Rust 设计决策：为什么启动序列是一个显式的阶段枚举

本章解释 `os/libs/minix-rt/src/start.rs` 文件里的每一个设计选择。

### 3.1 用显式的阶段枚举代替隐式的代码顺序

C 语言的启动顺序藏在函数体的行号顺序里：读者必须从第 150 行读到第 191 行才能拼出完整的顺序，而且顺序和代码混在一起，没有独立的名字。Rust 实现引入了 `StartupStage` 枚举，把八个阶段逐个命名：检查描述结构、发布环境变量、推导程序短名、运行库初始化、运行预初始化列表、登记关闭函数、运行初始化列表、调用主函数。`ordered` 函数返回这八个阶段的固定数组，顺序本身变成了可以测试的数据。测试只需要断言数组的长度是 8，第一个是检查描述结构，最后一个是调用主函数，就能锁定顺序不被意外打乱。这种手法的好处是顺序变化会立刻反映在测试失败里，而不是藏在代码审查的肉眼里。

替代方案是保留 C 风格的单一大函数，用注释标出每个阶段。这个方案被否决了，理由是注释和代码之间没有编译器检查，注释写错了不会报错，而枚举和数组写错了测试会立刻失败。

### 3.2 用哨兵判断函数代替裸的十六进制比较

C 语言的环境哨兵值 `0x53535353` 是一个裸的十六进制常量，散落在定义和使用两个地方。Rust 实现把它命名为 `UNINITIALIZED_ENVIRON_SENTINEL` 常量，并提供 `environ_is_uninitialized` 判断函数。常量名字解释了这个值的用途，函数名字解释了比较的意图，调用者不需要知道具体的十六进制数字。在 64 位地址空间里，这个 32 位哨兵占据地址的低半部分，高半部分是零，函数里直接比较完整的 64 位值，语义和 C 语言的低两字节比较完全一致，但是表达更直接。

### 3.3 用字节扫描代替字符解码

程序短名的推导在 C 语言里是逐字符扫描斜杠，Rust 实现用逐字节扫描斜杠。选择字节而不是字符的理由是路径不一定是合法的文本编码，字符解码在遇到非法字节序列的时候会报错或者替换，而 C 语言的行为是原样通过。字节扫描保证了任何字节序列都能得到和 C 语言完全相同的结果，包括空路径、根目录、连续斜杠、非文本编码这些边界情况，测试里对每一种情况都有覆盖。

### 3.4 用统一的函数列表运行器代替三套循环

C 语言里有三套几乎一样的循环：预初始化列表正序、初始化列表正序、老式构造器列表逆序。Rust 实现把它们合并成 `run_function_array` 函数，参数是函数切片和方向枚举。正序和逆序的区别只是一个迭代器方向，合并之后测试只需要覆盖两个方向，不需要为每个列表重复写测试。这种合并在重构文献里叫做消除重复，重复的循环体是未来分叉的温床，合并之后行为只有一处定义。

Redox 的链接器包做了类似的事情：它也把启动函数列表抽象成统一的运行器，区别在于 Redox 的列表来自动态链接器的重定位表，而 Minix 的列表来自静态链接的数组段。两种来源在运行时看起来都是连续的函数指针数组，所以运行器的形状可以共享，输入的来源不同而已。Rust 实现没有复制 Redox 解析重定位表的代码，因为静态链接的程序不需要它，这正是语义重写和逐行翻译的区别：共享思想，不共享代码。

### 3.5 用可拥有的单次守卫代替静态标志

C 语言的 `_libc_init` 用函数内部的静态标志保证只运行一次，标志的生命周期和整个进程一样长，测试没法重置它。Rust 实现把单次语义抽成 `RunOnce` 类型，调用者拥有这个值，第一次调用运行回调并返回真，之后的调用跳过回调并返回假。真正的二进制程序在运行时初始化模块里放一个全局的守卫，单元测试在每个测试里新建局部的守卫，测试之间互不干扰。类型很小，可以按值复制，语义和 C 语言的静态标志完全相同，但是可测试性完全不同。

---

## 4 错误处理：入口阶段的两种失败及其错误号

| Rust 错误 | 含义 | 对应的 Minix 错误号 | 选择理由 |
|-----------|------|---------------------|----------|
| `EntryError::MissingDescriptor` | 描述结构指针是空 | `ENOEXEC`（值 8） | 没有启动信息就没有合法的进程镜像，属于可执行文件格式错误 |
| `EntryError::InvalidCount` | 个数字段是负数 | `EINVAL`（值 22） | 参数向量的格式错误，C 语言库对这类故障统一使用参数错误 |

描述结构缺失在 C 语言里直接终止进程，Rust 版本把它变成显式错误，调用者可以在入口处把它变成进程退出码，效果相同但是路径可见。个数非负检查在 C 语言里不存在，因为 C 调用者默认内核不会给出负数；Rust 版本多做的这次检查对应进程字符串描述结构的构造器，错误号选择遵循 C 语言库对非法参数向量的一贯做法。

---

## 5 测试：11 个测试如何覆盖本模块的行为

本模块的测试全部位于 `os/libs/minix-rt/src/start.rs` 文件底部的测试模块中，运行命令是 `cargo test -p minix-rt --manifest-path os/Cargo.toml`。归属本模块的有 11 个测试：

| 测试函数 | 验证的行为 | 对应的 C 语言依据 |
|----------|-----------|-------------------|
| `test_null_descriptor_is_rejected` | 空描述结构指针被拒绝 | `crt0-common.c` 空指针检查 |
| `test_negative_counts_are_rejected` | 负数个数被拒绝 | 描述结构的有符号个数字段 |
| `test_valid_descriptor_keeps_all_fields` | 合法描述结构保留全部字段 | 描述结构定义 |
| `test_short_name_strips_directory_prefix` | 完整路径截出短名，空参数返回空字符串 | 短名推导循环 |
| `test_short_name_keeps_trailing_bytes_after_last_slash` | 非文本编码字节原样通过 | 字节比较语义 |
| `test_environ_sentinel_detection` | 哨兵值识别正确，空指针和真实地址不误判 | `environ.c` 哨兵定义 |
| `test_forward_array_runs_in_order` | 正序列表按登记顺序运行 | 初始化列表循环 |
| `test_reverse_array_runs_backwards` | 逆序列表从后往前运行 | 构造器兼容层循环 |
| `test_run_once_executes_exactly_once` | 单次守卫恰好运行一次 | `_libc_init` 静态标志 |
| `test_startup_order_contains_eight_stages_ending_with_main` | 启动顺序共 8 个阶段，首尾正确 | 入口函数行号顺序 |
| `test_entry_errors_map_to_documented_errnos` | 两种错误映射到文档记载的错误号 | 错误号取值 |

测试统计截至 2026 年 09 月 05 日：`cargo test -p minix-rt` 共 31 个测试通过，其中本模块 11 个。读者可以用 `rg "fn test_" os/libs/minix-rt/src/start.rs` 列出本模块的全部测试函数名，逐个核对。

---

## 6 过渡：从程序入口到运行时初始化

程序入口解决的是控制流的起点问题：汇编桩如何整理栈和寄存器，描述结构如何变成环境变量和程序短名，启动函数列表在什么时候运行，主函数的返回值如何变成退出码。下一个问题是运行时状态的发布问题：内核信息页面的指针如何从内核到达用户程序，全局的通信向量表如何从默认的直接陷阱实现切换到内核发布的实现，魔数校验失败的时候全局状态应该是什么样子，错误号全局变量和线程本地存储在 64 位系统上应该如何安排。

这些问题属于进程运行时生命周期的下一个阶段：运行时初始化。请继续阅读第 03 篇 `03-runtime-init.md`，它从构造器函数的三步流程讲起，逐个讲解通信向量表的选择逻辑、错误号模型的演进、线程本地存储的两种形态，并在 Rust 侧给出显式的初始化函数和可拥有的运行时状态类型。

---

## 7 参见

- `os/libs/minix-rt/src/start.rs`：本篇对应的 Rust 实现，包含描述结构、短名推导、函数列表运行器、单次守卫、阶段枚举和全部测试。
- `os/libs/minix-rt/src/handoff.rs`：第 01 篇的实现，描述结构体的定义来自它的进程字符串描述类型。
- `os/libs/minix-rt/src/init.rs`：第 03 篇的实现，真正的全局守卫和初始化入口在那里。
- `minix3/lib/csu/arch/x86_64/crt0.S`：汇编桩的原始实现，共 7 行指令。
- `minix3/lib/csu/common/crt0-common.c`：C 入口函数的原始实现，含环境发布、短名推导、动态检查、列表运行、主函数调用。
- `minix3/lib/csu/common/crtbegin.c`：构造器兼容层的原始实现，含逆序运行循环。
- `minix3/minix/lib/libc/sys/environ.c`：环境哨兵值的原始定义和注释。
- `minix3/lib/libc/misc/initfini.c`：C 语言库初始化函数的原始实现，含单次标志和各子系统初始化顺序。
- `minix3/sys/sys/exec.h`：进程字符串描述结构的原始定义。
- `notes/rewrite/fork-syscall-rewrite/14-stage-runtime/01-kernel-handoff.md`：前一篇，内核交付与初始栈。
- `notes/rewrite/fork-syscall-rewrite/14-stage-runtime/03-runtime-init.md`：后一篇，运行时初始化与通信向量表安装。
- `notes/rewrite/fork-syscall-rewrite/14-stage-runtime/plan.md`：本阶段的文档重组计划、架构演进清单和覆盖契约。
