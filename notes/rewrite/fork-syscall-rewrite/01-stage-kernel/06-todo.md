# 06-proc-init-boot-proc.md 重构需求文档（TODO / Review 交付物）

> **本文档用途**：这是一份**自包含的重构需求说明**，供多个 AI（Trae Code、Claude、GPT 网页版等）或人类审阅 / 执行。读者可能**无法访问本仓库的代码与上下文**，因此本文档把背景、前因、方案、验收标准全部写入正文。审阅者 / 执行者应以本文档为唯一依据，必要时再对照仓库源码。
>
> **目标文件**：`notes/rewrite/fork-syscall-rewrite/01-stage-kernel/06-proc-init-boot-proc.md`（下文简称「06」）
>
> **状态**：v2.1 **定稿**（2026-08-29 GPT 终审通过：「可以定稿，开始执行，不要再继续设计层面迭代」；执行注意事项见 §8 开头，执行顺序三阶段见文末摘要第 9 条）
>
> **执行记录（2026-08-29）**：三阶段执行完毕 ✅。第一阶段（Step 0–2）：22-privilege KPriv 计数修正 6→8、Step 1 旧→新映射表、`.design/06-outline.v2.md` 冻结。第二阶段（Step 3–8）：06 正文按 outline 重写完毕（Ch1 精简 / Ch2 存储模型分组 / Ch3 决策重编号 §3.0–§3.13 / Ch4 含 §4.9 FPU / Ch5 证据外移 / 附录 A 删除 / Ch6 职责边界矩阵）。第三阶段（Step 9）：A1–A15 全部通过——A9b 修正 2 处下游锚点漂移（01-boot-shim §2.5→§2.3、10-switch-to-user §3.5→§3.12）；A1 行数 1785 > 1200，必要性说明：篇幅来自「字段全集导航表」（v2.1 校准要求保留）+「三架构对照」（arch 抽象强制约束）+「12 步填充节拍」（A15 主线完整），单一概念承载，非概念混杂复发。
>
> **编写日期**：2026-08-29（v2 同日修订）

---

## 目录

1. [项目背景](#1-项目背景)
2. [文档体系约定](#2-文档体系约定)
3. [06 现状与问题](#3-06-现状与问题)
4. [决策过程：为什么拆又为什么不拆](#4-决策过程为什么拆又为什么不拆)
5. [解决方案总览](#5-解决方案总览)
6. [核心改造：字段分组 + 后续文档引用](#6-核心改造字段分组--后续文档引用)
7. [目标文档结构（detailed outline）](#7-目标文档结构detailed-outline)
8. [分步执行工作项](#8-分步执行工作项)
9. [验收标准](#9-验收标准)
10. [约束与注意事项](#10-约束与注意事项)
11. [附录：后续文档内容证据](#11-附录后续文档内容证据)

---

## 1. 项目背景

**Minix-RS** 是一个把 Minix 3（微内核操作系统）的 C 代码**语义等价重写为 Rust** 的项目：

- **上游源码（ground truth）**：Minix 3 的 C 源码，位于 `minix3/` 目录，**只读不改**。
- **Rust 实现**：位于 `os/` 目录下的各 crate（`os/kernel` 内核、`os/arch` 架构层、`os/servers/vm` VM 服务等）。
- **重写原则**：不是逐行翻译（translation），而是**语义重写**（semantic rewrite）——保持外部行为、IPC 协议、生命周期语义与 Minix3 一致，但用 Rust 类型系统重新表达内部结构。

项目的关键约束（与本文档强相关）：
- **`#![no_std]` + boot 期无堆**：内核代码除 `#[cfg(test)]` 外不依赖标准库；且 kernel **boot 阶段堆分配器尚未建立**（注意：`no_std` ≠ 没有 allocator——no_std 环境可以自带分配器，这里的原因是 boot 期未建立），因此核心表必须用固定容量静态存储（`static [T; N]`）而不是 `Vec` / `Box`。这是本文档方案的核心动因之一，完整三层因果链见 §10.5。
- **执行模型分层**：用户态服务器是单线程事件循环；**内核是 SMP + BKL（Big Kernel Lock）**，跨 CPU 共享数据需要 `Atomic` / 锁，进程表的 `p_rts_flags` 在 Rust 侧是 `AtomicU32`。
- **文档体系**：`notes/rewrite/fork-syscall-rewrite/01-stage-kernel/` 下按「阶段 + 功能」组织 32+ 篇概念文档，每篇对应一组 Minix3 内核 C 源码 / Rust 实现。用**启动主线**串联整个内核启动流程（阶段 A–F）。

---

## 2. 文档体系约定

`01-stage-kernel/` 目录的文档编号即阅读顺序，也是**启动主线**：

| 阶段 | 文档 | 主题 |
|------|------|------|
| A（引导） | `01-boot-shim-bootstrap.md` | bootloader 接管，加载内核镜像；multiboot 协议 |
| B（早期初始化） | `03-kmain-cstart.md`、`04-platform-discovery.md`、`05-clock-interrupt-init.md` | kmain/cstart、平台探测、时钟中断 |
| **C（进程表初始化）** | **`06-proc-init-boot-proc.md`（本文档改造目标）** | **清空进程表/特权表 → 遍历 boot image → 加载 VM ELF** |
| D（跨空间） | `07-cross-space-init.md` | direct_map 就绪 |
| E（系统服务） | `08-system-init-boot-finish.md`、`09-vm-boot-protocol.md` | 系统调用注册、bsp_finish_booting、VM 协商 |
| F（首次切换） | `10-switch-to-user.md` | 调度器选中第一个用户进程 |

**「功能」文档**（06 的后续承接方）：

| 文档 | 主题 | 与 06 的关系 |
|------|------|-------------|
| `11-scheduling-primitives.md` | 调度原语 enqueue/dequeue/pick_proc；RTS 状态机与就绪队列一致性 | 06 讲 RTS 位图「存储 + 位全集 + 不变量」；11 讲「enqueue 联动」细节 |
| `12-ipc-core.md` | IPC 六原语、SENDING/RECEIVING 状态机、delivermsg | 06 讲进程表 IPC 字段分组；12 讲 IPC 运行时行为 |
| `17-syscall-process.md` | fork/exec/exit/clear 等进程生命周期系统调用 | 06 讲进程表字段布局 + boot 期初值；17 讲字段运行时读写（fork 继承等） |
| `22-privilege.md` | 权限模型：struct priv 31 字段 6 组、三类掩码 | 06 讲特权表「存储布局 + 分治策略」；22 讲字段语义 + CapabilityTemplate 用法 |
| `19-syscall-signal.md`、`20-syscall-device.md`、`21-syscall-clock.md`、`23-ipc-filter.md`、`24-cross-space-runtime.md` | 信号 / 设备 / 时钟 / IPC 过滤 / 跨空间 | 06 的 priv 字段各组按引用指向它们 |

**关键约定**：文档之间**「先总后分、描述 + 引用」**——主文档（06）建立数据结构的概念模型与存储布局，把「运行时字段的具体读写」交由对应的功能文档展开，通过「详见 `NN-xxx.md §X.Y`」引用衔接。这与同仓库 `02-stage-vm/` 的成熟模式一致（`02-vmproc-struct.md` 讲 struct、`03-vmproc-table.md` 讲表、`04-acl.md` 讲单个子结构，文档间互相引用）。

---

## 3. 06 现状与问题

### 3.1 现状

- **篇幅**：2192 行，远超 `01-stage-kernel` 的常规体量（多数文档 400–900 行；全目录最长为 `05-clock-interrupt-init.md` 2253 行，暂不在本次范围）。
- **结构**：Ch1 概念（§1.1–1.7）→ Ch2 C 源码分析（§2.0–2.9）→ Ch3 Rust 设计决策（§3.0–3.15）→ Ch4 实现详解（§4.0–4.8）→ Ch5 测试 → 附录 A libexec 详解 → Ch6 参见。
- **内容覆盖**：进程本质、CPU 四问、三类运行态实体、进程表/特权表/RTS 三件套、boot image、VM 鸡生蛋、阶段 C 执行节拍、`proc_init`、`arch_proc_reset`、boot 循环、`get_priv`/`set_sendto_bit`、`arch_boot_proc`、`arch_proc_init`、`libexec_load_elf`、`bsp_finish_booting`、KPriv 8 子结构、CapabilityTemplate、RTS 16 位、ProcKind/EntrySpec、CpuContextArch trait、FPU 演进、SMP 预留、错误处理、fork 对照、测试矩阵。

### 3.2 问题诊断

**问题 1：单个文档混入了太多概念（v2.1 修订：行数只是症状，概念混杂才是病根）。**

- **行数本身不是问题**——如果 06 只承载一个确实很难的独立概念，2000 行甚至更长都可接受。真正的问题是 06 同时承载了：进程本质教学、三件套存储、boot image、VM 鸡生蛋、运行时字段用法、FPU 演进、fork 对照、libexec 详解……**每个概念都没讲好，因为每个都没讲透就切换到了下一个**。
- 2192 行（约为其他文档的 2–3 倍）与信息密度不均（入门级概念与论文级细节混杂，如 CPU 断点续传 vs KPriv 子结构字段表/libexec 回调签名）都是概念混杂的**症状**。改造的验收对象是**概念边界**（A1），不是行数。

**问题 2：把「存储布局」和「运行时字段用法」揉在一篇里，职责越界。**

- 06 既讲了「`struct proc` 有哪些字段、进程表怎么存」（存储层），又展开了「字段在 fork/exec 里怎么改、RTS 怎么联动调度队列」（运行时用法）。
- 后者的正确归属是后续功能文档（17/11/12/22）——它们已经存在且内容覆盖完整。06 的展开是**重复讲述**，且把文档拉长。
- 这正是用户最核心的反馈：**「启动的过程遇到了 proc table，把所有字段的讲解全部融入到一篇文档中，这就有点过大从而难以讲好了。」**

**问题 3：内核「必须讲清楚 table 怎么存」这一点被淹没。**

- 用户指出：用户态程序存一个表可以用 `Vec::new()`，一句话带过；但内核 **no_std + boot 期零堆**，进程表、特权表怎么存（静态数组 / 指针数组 / 容量常量）是**硬约束驱动的设计决策**，必须讲清楚。
- 06 现有 Ch2/Ch3 确实覆盖了 `static [KProcess; N]` 等设计，但**没有以「存储」为主线组织**——存储决策散落在逐函数分析中，读者很难抓住「为什么是这种存储形态」的因果链。

### 3.3 一句话总结（v2 修订：主从关系锁死）

> **06 是「阶段 C 启动主线」文档：解释 Minix3 如何在阶段 C 建立第一个可运行的进程运行环境——先建立 proc/priv/RTS 等核心表的存储模型（怎么存、为什么这么存），再沿 `proc_init → boot image → ELF load → wakeup` 展示这些结构如何被初始化并最终交给后续运行时；字段按分组建立导航，运行时细节引用后续文档。** 篇幅预期 ~800–1000 行（**纯参考值，不作为 pass/fail 指标**——行数不是问题，概念边界才是，见 §9 A1）。

**主从关系**：阶段 C 启动主线是**第一主线**，存储模型是为理解启动而服务的**支撑**——但支撑部分（内核为什么这样存）必须讲透，不得浅尝辄止（见 §4.4 两条红线）。

---

## 4. 决策过程：为什么拆又为什么不拆

### 4.1 最初方案：拆成 5 篇（已否决）

在分析早期，对照 `02-stage-vm/` 的拆分模式（vmproc struct / vmproc table / acl 各一篇），曾考虑把 06 拆成：

- `06a-proc-table.md`（进程表）
- `06b-priv-table.md`（特权表）
- `06c-rts-bitmap.md`（RTS 位图）
- `06d-boot-image.md`（boot image）
- `06e-vm-elf-load.md`（VM ELF 加载）

**否决理由**：
1. **阶段 C 是一个完整主题**，不是三件独立数据结构的堆叠——它的主线是「清空 → 填充 → 唤醒」的**启动节拍**，拆分会把主线打散。
2. **后续功能文档已存在且覆盖完整**（11/12/17/22 等）。拆出 06a/06b/06c 会把「存储布局」和「运行时用法」**都各拆一遍**，反而增加文档间跳转成本，且与已有功能文档构成重复。
3. **拆分会破坏编号**：06 是启动主线上的固定锚点，下游文档（07/08/10/12 等）大量引用「06 §X.Y」；拆成 06a/06b… 会让所有交叉引用失效或歧义。

### 4.2 最终方案：不拆，瘦身 + 分组引用

**核心判断**：06 的「大」和「讲不好」不是因为它装了 6 个概念，而是因为它**把存储、设计、运行时三条线全部展开讲了一遍**。解法不是拆文档，而是**重划职责边界**：

| 层 | 谁负责 | 06 的职责 |
|----|--------|----------|
| **存储布局**（字段定义、容量、索引、共享策略、no_std 约束） | **06** | ✅ 本文档改造后重点 |
| **Rust 设计**（类型、trait、子结构分组） | **06 + 22** | 06 讲「存储形态」的设计决策；22 讲「权限语义」的设计 |
| **运行时字段用法**（fork 怎么改字段、enqueue 怎么用 p_nextready、IPC 怎么用 p_caller_q） | **17 / 11 / 12 / 22 / 19 / 20 / 21 / 23 / 24** | ❌ 06 只给引用 |
| **启动主线**（proc_init → boot 循环 → bsp_finish_booting） | **06 保留骨架** | ✅ 保留但不展开细节 |

**核心原则（用户原话，纳入正式需求）**：

> - 讲解 proc table，可以列出 `proc` struct 的整体布局，甚至列举所有字段，**但不展开整体讲解**，仅给出后续文档的引用。
> - 不讲解字段 ≠ 完全不讲解。**字段应该分组**，然后讲解**每个分组具体完成什么操作系统的语义**；具体完成细节归属到后续某个文档（以及 Rust 关于该语义、该分组如何设计）。
> - **priv 表同理。**

### 4.3 为什么「内核必须讲怎么存」是 06 存在的最大理由

用户的关键洞察（正式纳入需求）：

> 「`[]struct` 就是 table……但牵涉到在内核中如何存储，**std 下完全不用讲 `vec::new()` 就完事，内核必须讲清楚这个 table 怎么存**。」

对照表（06 必须写的存储决策 vs 用户态微不足道的等价物）：

| 内核存储决策 | 用户态等价 | 内核必要性 |
|-------------|-----------|-----------|
| `static [KProcess; NR_TASKS + NR_PROCS]` | `Vec::new()` | boot 期无堆分配器 → 必须静态数组 |
| `ppriv_addr: [Option<NonNull<KPriv>>; NR_PROCS]` | `HashMap<u32, &Priv>` | O(1) 索引 + 零堆 + 无 unsafe |
| `priv[NR_SYS_PROCS = 64]` 固定容量 | `Vec<Priv>` | 编译期容量已知 → `const fn new()` |
| `p_rts_flags: AtomicU32` | `u32` | SMP 跨 CPU 原子读 |
| `Endpoint(pub i32)` newtype | `type Endpoint = i32` | 跨 FFI 边界 + 类型安全 |
| `[T; N]` 用 `RTS_SLOT_FREE` 位表达「空」 | `Option<T>` 或 `Vec` | 零堆 + 无 Option 包装的静态数组 |

06 的 Ch2（或改造后的对应章节）必须把这些「为什么是这种存储形态」写清楚。

### 4.4 心智模型锁死：Stage C 是第一主线，存储是支撑（v2 新增）

**06 是「Stage C 的文档」，不是「proc/priv/RTS 的总文档」。** proc/priv/RTS 只是阶段 C 为了建立第一个可运行环境而操作的三类核心状态——不能写成四个主题的拼盘：

```
06：阶段 C —— 建立第一个可运行的进程运行环境
        │
        ├── 需要什么数据结构？          → proc table / priv table / RTS / boot image
        ├── 这些结构如何组织、为什么？    → 存储因果链（Ch2/Ch3 讲透）
        └── 如何用它们完成启动节拍？      → 清空 → 填充 → 加载 → 唤醒（Ch1 §1.5 + Ch4）
```

**两条红线**（防摆向任一极端）：

1. **不得让字段/结构喧宾夺主**：字段分组是导航，启动节拍（清空→填充→加载→唤醒）是叙事主线——读者读完应能独立回答「阶段 C 做了什么、这些表为什么长这样」。
2. **不得把存储讲浅**：用户核心洞察「内核必须讲清楚这个 table 怎么存」依然成立——存储因果链在 Ch2/Ch3 内**完整论证**（这是 06 区别于用户态文档的存在理由），只是不展开运行时字段用法。

---

## 5. 解决方案总览

**保持 06 为单文档，重写内容组织。篇幅预期 ~800–1000 行（纯参考值，非验收指标）——真正的质量闸门是**概念边界**（06 只承载「阶段 C 启动主线 + 其存储模型」一个核心概念，A1）与 §9 的 A13（反重复）/ A14（反百科）/ A15（启动主线完整性）。**

保留 / 精简 / 删除的内容规划：

| 现有章节 | 处置 | 说明 |
|---------|------|------|
| Ch1 §1.1 进程本质 | **保留** | 断点续传、进程五部分组成、三类运行态实体——06 独有概念总览，其他文档依赖 |
| Ch1 §1.2 CPU 四问 | **保留（可精简）** | 寄存器初值/入口/栈和参数/地址空间，是阶段 C 的概念骨架 |
| Ch1 §1.3 三件套 | **重写** | 改为「每件 30 行建模型 + 引用」：进程表(→17/11)、特权表(→22)、RTS(→11)。**当前过度展开，需按 §6 的分组方案重写** |
| Ch1 §1.4 boot image / VM 鸡生蛋 | **精简** | 压到 ~60 行：保留 image[] 概念 + proc_nr 编号表 + multiboot 关系一句，细节指向 01/08/09 与 02-stage-vm |
| Ch1 §1.5 阶段 C 执行节拍 | **保留** | 启动主线骨架：清空→填充→唤醒，每步标注代码锚点 + 后续文档指针 |
| Ch1 §1.6 boot 流程终点 | **精简** | 唤醒动作列表压缩，细节指向 08 |
| Ch1 §1.7 小结 | **保留** | |
| Ch2 C 源码分析（§2.0–2.9，~640 行） | **重构为「C 源码分析：Minix3 数据结构与存储模型」**（章节名保契约，主题作副标题） | 按 §6 的分组方案重组 = 06 核心章节。**分组模板纯 C 侧**（字段 + C 结构关系 + 阶段 C 作用），Rust 表达一律去 Ch3（v2 修订）。逐函数展开（arch_boot_proc、libexec 等）下放或压缩 |
| Ch3 Rust 设计决策（§3.0–3.15，~470 行） | **重写为「按分组的 Rust 设计决策」** | 与 Ch2 分组**概念对应**（v2.1 修订：仅对存在独立 Rust 设计决策的分组建立小节，无独立决策的组在汇总表一行带过）；每组「C 结构 → Rust 表达 → 为什么 → 与 C 差异」~30 行；删除与 22 重复的 KPriv 详细字段（前置依赖：Step 0 先修 22 的 KPriv 计数） |
| Ch4 实现详解（§4.0–4.8，~280 行） | **保留主流程部分**（章节名保契约；阶段 C 启动主线的落点） | §4.0 实现地图 + §4.7 init_proc_and_boot 主流程保留**完整**（清空→填充→加载→唤醒节拍不得砍薄，验收 A15）；§4.1/4.2 arch trait 实现可压缩 |
| Ch5 测试要点 | **保留** | 现有覆盖矩阵完整 |
| 附录 A libexec 详解（~100 行） | **删除**（可选：移到 02-stage-vm 相关文档或压缩为一段） | Rust 用 `minix_elf` crate，C 框架细节对主线读者价值低 |
| Ch6 参见 | **保留并扩充** | 加入对 11/12/17/22/19/20/21/23/24 的引用 |

---

## 6. 核心改造：字段分组 + 后续文档引用

> 这是整份需求最核心的部分。改造后的 06 Ch2 以「分组」为单元组织。**每个分组 = 一组完成同一 OS 语义的字段 + 该语义的一句话 + 后续文档引用。** 字段可以全部列出（导航表），但不展开每个字段的运行时细节。

**v2 关键修订——C/Rust 分层（防 Ch2/Ch3 打架）**：Ch2 与 Ch3 职责严格分层，两组模板分开：

- **Ch2 模板（纯 C 侧）**：字段 → OS 语义 → **C 中的结构关系** → **阶段 C 中承担的作用** → 后续文档引用。**禁止出现 Rust 类型**（newtype/Atomic/VecDeque 等）。
- **Ch3 模板（Rust 侧）**：C 结构 → **Rust 表达** → **为什么这样表达** → **与 C 的差异**。与 Ch2 分组**概念对应**（v2.1 修订：非机械镜像——仅对存在独立 Rust 设计决策的分组建立独立小节；无独立决策的组在 Ch3 汇总表中标「沿用 C 形态/阶段 C 仅零初始化」即可，不强行建节）。

§6.1 分组表中出现的「Rust 侧 …」提示文字仅作为 Ch3 对应节的**索引**，正文不得在 Ch2 展开。

### 6.1 进程表（`struct proc`）字段分组

ground truth：`minix3/minix/kernel/proc.h:22-`（struct proc 约 60+ 字段，含嵌套结构）。建议分为以下几组：

| 分组 | 代表字段 | 完成的 OS 语义 | 后续文档引用 |
|------|---------|--------------|-------------|
| **标识组** | `p_nr`、`p_endpoint`（含 generation）、`p_name`、`p_magic`（如有） | 「我是谁、谁能找到我」；endpoint 是 generation 化身份，slot 是数组索引 | 17 §1.1–1.2（生命周期、fork 代际递增） |
| **调度属性组** | `p_priority`、`p_quantum_size_ms`、`p_cpu_time_left`、`p_nextready`、`p_scheduler`、`p_cpu`（+SMP `p_cpu_mask`） | 「什么时候跑、跑多久、在哪个 CPU 跑」 | 11 §2（enqueue/dequeue/pick_proc） |
| **IPC 状态组** | `p_getfrom_e`、`p_sendto_e`、`p_caller_q`、`p_q_link`、`p_sendmsg`、`p_delivermsg`、`p_delivermsg_vir`、`p_pending` | 「在等谁、谁在等我、消息怎么投递」 | 12 §1（IPC 六原语、delivermsg）、17 §1.2（fork 同步前提） |
| **执行上下文组** | `p_reg`（stackframe_s 用户寄存器全栈）、`p_seg`（段描述符）、`p_vmrestart` 系列（VM 请求保存） | 「被暂停时 CPU 长什么样」；决定首次 boot 时寄存器初值（arch_proc_reset） | 10 §1（switch_to_user 恢复）、09 §1（VM 请求协议）、31（FPU） |
| **记账/统计组** | `p_accounting{enter_queue,time_in_queue,dequeues,ipc_sync,ipc_async,preempted}`、`p_dequeued`、`p_user_time`、`p_sys_time`、`p_virt_left`、`p_prof_left`、`p_cycles`、`p_kcall_cycles`、`p_kipc_cycles`、`p_tick_cycles`、`p_cpuavg` | 「用了多少资源、给调度员看什么账」 | 17 §1.1（生命周期收尾记账）、11 §3（sched_proc 参数更新） |
| **生命周期控制组** | `p_rts_flags`（16 位位图，全部列出）、`p_misc_flags`、`p_priv`（指向特权槽） | 「现在能不能跑 + 有什么权限」；不变量：`p_rts_flags == 0 ⟺ 可运行` | 11 §1.1（不变量与队列一致性）、22 §1（priv 指针） |

**Ch2 每组的写作模板（v2 修订：纯 C 侧，示例为标识组）**：

```markdown
#### 2.1.1 标识组：进程的身份

字段：`p_nr` / `p_endpoint` / `p_name` / `p_magic`（以 proc.h 为准）

**OS 语义**：内核需要一个稳定、O(1) 可寻址的标识把「进程」与「系统消息、调度、信号」关联起来。

**C 中的结构关系**：
- `p_nr`：slot 索引偏移（负数=内核 task，非负=用户进程），`proc_addr(n)` 宏用数组下标 O(1) 定位——「身份即索引」，这是 C 静态数组方案的基石。
- `p_endpoint`：generation 化身份 `(generation, p_nr)`，防止 slot 复用后的「陈旧引用」（A 持旧 endpoint 给已退出 B 发消息，被 generation 不匹配挡住）。
- `p_magic`：slot 完整性哨兵（如有，待对照 proc.h 确认）。

**阶段 C 中的作用**：`proc_init` 清空时重排 p_nr；boot 循环按 p_nr 落槽。

**运行时细节**（哪些操作会改这些字段、endpoint 代际怎么递增）：详见 [17-syscall-process.md §1.1–1.2](./17-syscall-process.md)。

（对应 Rust 设计 `ProcNr`/`Endpoint` newtype：见 §3.1 标识组——不在本节展开。）
```

**Ch3 对应组的写作模板（v2 新增）**：

```markdown
#### 3.1.1 标识组的 Rust 表达（与 §2.1.1 概念对应）

**C 结构**：`p_nr: int` + `p_endpoint: int`（裸 int，二者可混淆）。

**Rust 表达**：`ProcNr(i32)`、`Endpoint(i32)` 两个 newtype（`repr(transparent)`），类型层面无法混淆。

**为什么**：endpoint 与 proc_nr 是不同概念空间的名字（endpoint 可含 generation），裸 int 无法在编译期阻止「拿 p_nr 当 endpoint 用」的错误。

**与 C 的差异**：仅类型安全增强，外部行为（endpoint 数值、generation 递增时机）不变。
```

**「导航，不是百科」规则（v2 新增，防「字段百科的压缩版」回潮）**：

1. **字段全集可全列**——按分组整理成紧凑导航表（6 组合计 ~20 行），这是导航索引，不是逐字段讲解。
2. **展开只限阶段 C 真正触碰的字段**：`p_nr` / `p_endpoint` / `p_priv` / `p_rts_flags` / `p_reg` / `p_seg`（+ boot image 映射相关字段）。
3. **非阶段 C 必需字段只归类不展开**：如记账组（`p_cpuavg`/`p_kcall_cycles`/`p_kipc_cycles`…）一句「属于调度记账语义，阶段 C 仅清零，运行时语义见 11 §3」即可，不逐字段列表。
4. **每组末尾必须有转交引用**——读者的问题「这些状态去哪继续读」由引用回答，不由 06 展开。
5. **「OS 语义」只回答「这一组字段代表哪类状态」，不得展开该状态的完整运行时协议**（v2.1 新增）：如果某字段在阶段 C 仅被清零、赋初值或挂接，「阶段 C 作用」只描述该初始化动作，不追溯其后续运行时行为——防止把「调度属性组」写成半吊子 scheduler 教程（GPT 复审 P0）。
6. **Ch3 概念对应，不机械镜像**（v2.1 新增）：仅对存在独立 Rust 设计决策的分组建立 Ch3 小节；没有独立决策的组（如记账/统计组，阶段 C 仅 memset 清零）在 §3.6 汇总表一行带过，不为凑对应关系强行写无价值的设计小节。

### 6.1b 职责边界矩阵（v2 新增，防未来文档再次膨胀）

| 问题 | 06 | 11 | 12 | 17 | 22 | 其他 |
|------|----|----|----|----|----|------|
| proc/priv/RTS 表怎么存（C 形态 + Rust 表达） | ✅ | | | | 引用 | |
| 字段有哪些、语义分组索引 | ✅ 导航 | | | | | |
| RTS 不变量（runnable iff 0） | ✅ 口号 | ✅ 联动细节 | | | | |
| enqueue/dequeue/pick_proc | | ✅ | | | | |
| IPC 状态机 / delivermsg | | | ✅ | | | |
| fork/exec/exit 修改 proc 字段 | | | | ✅ | | |
| endpoint generation 递增 | | | | ✅ | | |
| priv 字段语义 / 三类掩码 | 引用 | | | | ✅ | |
| boot image 概念与编号表 | ✅ | | | | | 01/08/09 细节 |
| ELF 加载细节 | 骨架 | | | | | 02-stage-vm |
| VM boot protocol | 引用 | | | | | 09 |
| switch_to_user / 首次切换 | 引用 | | | | | 10 |
| FPU 架构演进 | 引用 | | | | | 31 |

此矩阵写入 06 的 Ch6（参见），作为「先总后分」约定的可视化锚点。

### 6.2 特权表（`struct priv`）字段分组

ground truth：`minix3/minix/kernel/priv.h:20-65`（struct priv 31 字段）。建议沿用 22-privilege.md §1.1 已有的 6 组划分（C 端字段按职责分组，保持一致性；22 §4.2 Rust 端是 **8 子结构**——见 §6.2.0「存储策略」段说明两者如何对应）：

| 分组 | 代表字段 | 完成的 OS 语义 | 后续文档引用 |
|------|---------|--------------|-------------|
| **身份/能力组** | `s_proc_nr`、`s_id`、`s_flags`（PREEMPTIBLE/BILLABLE/SYS_PROC/ROOT_SYS_PROC/VM_SYS_PROC…）、`s_init_flags` | 「这个特权槽属于谁、是什么角色」 | 22 §1.2（s_flags 位表） |
| **信号组** | `s_sig_mgr`、`s_bak_sig_mgr`、`s_notify_pending`、`s_asyn_pending`、`s_int_pending`、`s_sig_pending` | 「谁是我的信号管理器、挂起哪些信号/通知」 | 19（信号投递路径） |
| **IPC 组** | `s_trap_mask`、`s_ipc_to`、`s_k_call_mask` | 「允许触发哪些 trap、能给谁发 IPC、能调哪些内核调用」；注意 `may_send_to` 无条件检查 `s_ipc_to` | 22 §1.3（三类掩码）、23（IPC 过滤）、12 |
| **I/O 组** | `s_nr_io_range`、`s_io_tab`、`s_nr_irq`、`s_irq_tab` | 「能访问哪些 I/O 端口、处理哪些 IRQ」 | 20（设备驱动类 syscall） |
| **内存组** | `s_nr_mem_range`、`s_mem_tab`、`s_ipcf`、`s_stack_guard`、`s_diag_sig` | 「能访问哪些内存范围、IPC 过滤规则、栈保护」 | 24（cross-space-runtime）、23 |
| **运行时组** | `s_alarm_timer`、`s_grant_table`、`s_grant_entries`、`s_grant_endpoint`、`s_state_table`、`s_state_entries`、`s_asyntab`、`s_asynsize`、`s_asynendpoint` | 「闹钟、grant 表、state 表、异步发送表运行时挂载」 | 21（syscall-clock）、17（grant/statectl） |

**特权表「存储」必须单独讲的部分**（§2.2.0）：

- **分治策略**：进程表有 `NR_PROCS`（数百）个槽，但特权表只有 `NR_SYS_PROCS = 64` 个槽。只有系统进程需要独立特权，普通用户进程**共享单一 `USER_PRIV`**。如果把 31 字段平铺到每个进程，内存浪费巨大。`p_priv` 指针（进程表）→ 所属特权槽。
- **双表索引结构**：`priv[NR_SYS_PROCS]`（实体数组）+ `ppriv_addr[NR_PROCS]`（指针数组，`priv_addr(i) = ppriv_addr[i]`），进程号 → O(1) 找特权槽。两表长度不同、以指针数组桥接。
- **零堆的存储形态**（C 侧）：`priv[NR_SYS_PROCS]` 静态数组 + `ppriv_addr[NR_PROCS]` 指针数组在编译期固定大小。Rust 端 `PrivTable` 的具体表达（`[KPriv; NR_SYS_PROCS]` + `const fn new()`）属 Ch3 §3.2，本节（Ch2）只论证「为什么必须静态数组」。**这是「内核必须讲怎么存」的特权侧体现。**
- **boot 期静态分配 vs 运行时动态分配**：boot 期只为内核 task + RS + VM 分配静态特权（`NR_STATIC_PRIV_IDS` 段）；运行时 `get_priv()` 从动态段（`[NR_BOOT_PROCS.. NR_SYS_PROCS)`）分配。

### 6.3 RTS 位图存储（可并入 6.1 生命周期控制组，或独立一节）

- **为什么位图而非枚举**：进程可同时因多个原因不可运行（无特权 + 等 VM 建页表 + 被停止），位图支持多原因叠加。
- **16 位全集全部列出**（`SLOT_FREE`/`NO_PRIV`/`NO_QUANTUM`/`VMINHIBIT`/`BOOTINHIBIT`/`PROC_STOP`/`SENDING`/`RECEIVING`/`SIGNALED`/`SIG_PENDING`/`P_STOP`/`NO_ENDPOINT`/`PAGEFAULT`/`VMREQUEST`/`VMREQTARGET`/`PREEMPTED`）。
- **不变量**：`p_rts_flags == 0 ⟺ runnable`；`rts_set`/`rts_unset` 联动 enqueue/dequeue（06 只讲不变量，联动细节 → 11）。
- **存储决策（v2 修订：现存已验证设计，非预设）**：`RtsFlags` 为 `AtomicU32` + bitflags 宏（已实现于 [os/kernel/src/proc_table.rs](file:///os/kernel/src/proc_table.rs) / [os/kernel/src/sched.rs](file:///os/kernel/src/sched.rs)）。**并发协议论据必须写实，分三段**：

  1. **概念结论**：BKL 串行化所有写者；`AtomicU32` 支撑跨 CPU 无锁读。
  2. **具体代码证据**（写侧）：
     - `p.p_rts_flags.set(RtsFlagsBits::NO_QUANTUM)` — [sched.rs:366](file:///os/kernel/src/sched.rs#L366)
     - `p.p_rts_flags.clear(RtsFlagsBits::NO_QUANTUM)` — [sched.rs:408](file:///os/kernel/src/sched.rs#L408)
     - `table.rts_set(ProcNr(0), RtsFlagsBits::PROC_STOP)` — [sched.rs:503](file:///os/kernel/src/sched.rs#L503)
     这些都通过 `rts_set`/`rts_unset` 封装（[sched.rs:329-410](file:///os/kernel/src/sched.rs#L329-L410)），被调度器在持 BKL 期间调用。
  3. **代码中的实际对象**（无锁读模式参考）：**注意**——`p_rts_flags` 本身在代码里**不直接**暴露 `load(Relaxed)`（bitflags 宏的 `.is_set()` 包装了 load）。同表上的 `p_nextready` 才是显式 `AtomicI32` + `load(Ordering::Relaxed)` 模式（[sched.rs:153, 167](file:///os/kernel/src/sched.rs#L153, #L167)），用于调度器跨 CPU 扫描就绪队列（idle-steal 路径）。**RTS 自身的「跨 CPU 读」是通过 bitflags API（`.is_set()`）间接表达**，调用方通常在持 BKL 内使用，因此 `Relaxed` 序不是 RTS 字段本身的硬约束，而是 `p_nextready`（队列链接）的硬约束。
  4. **结论**：BKL 串行化与 Atomic 是两个维度——前者管写互斥（防止并发修改 `p_rts_flags`），后者管**同表相邻字段**（如 `p_nextready`）的读可见性/借用模型。RTS 字段本身在持 BKL 临界区内使用 `AtomicU32` 是防御性设计（编译期杜绝裸 `u32`，并为 `SyncUnsafeCell` 包裹的全局 `PROC_TABLE` 提供安全读侧）。**不得写成「SMP → 所以 Atomic」的口号**——必须区分「写侧 BKL 串行化」与「读侧无锁 / 跨字段链接原子性」两个不同维度。
- **boot 期涉及的 6 位**（阶段 C 视角）：`SLOT_FREE`/`NO_PRIV`/`NO_QUANTUM`/`VMINHIBIT`/`BOOTINHIBIT`/`PROC_STOP`，说明每位的设置时机。

### 6.4 boot image 存储（保留精简版）

- `image[]`（`NR_BOOT_PROCS = NR_TASKS + NR_BOOT_MODULES = 5 + 12 = 17`）是编译时硬编码清单，**不是 ELF**，编译进 kernel 二进制。
- proc 号编号表（DS=6, RS=2, PM=0, …）——boot image 顺序 ≠ proc 号顺序，决定槽位落点。Rust 侧 `BOOT_MODULE_PROC_NRS[i]`。
- multiboot module list（`kinfo.module_list`）提供 ELF 物理地址；boot image order 与 module list 通过 `i - NR_TASKS` 对应。
- 详情指向 01 §1.5、08 §1.3、09 §1.2。

---

## 7. 目标文档结构（detailed outline）

改造后 06 的建议结构（~800 行参考值）：

```
06-proc-init-boot-proc.md
├─ 头部元数据（阶段/range/ground truth/Rust 实现/前置/后续）   （保留，扩充后续列表）
├─ Ch1 概念与定位（Reader orientation，~150 行）
│  ├─ §1.0 本章讲什么（阶段 A–F 表，保留）
│  ├─ §1.1 进程的本质（断点续传、五组成部分、三类运行态实体；控制篇幅，够理解阶段 C 即可）
│  ├─ §1.2 CPU 要回答的四个问题（精简）
│  ├─ §1.3 三件套总览（重构：每件 30 行模型 + 引用）
│  ├─ §1.4 boot image 与 VM 鸡生蛋（精简到 ~60 行）
│  ├─ §1.5 阶段 C 的执行节拍：清空→填充→加载→唤醒（保留，链接 Ch2 + 后续文档——启动主线的概念落点）
│  ├─ §1.6 boot 流程终点：唤醒动作（精简，细节指向 08）
│  └─ §1.7 本章小结（保留）
├─ Ch2 C 源码分析：Minix3 数据结构与存储模型（核心章节, ~300 行）  ← 本文档 §6 的落点；分组组织，纯 C 侧
│  ├─ §2.0 存储约束总述（三层因果链：Minix 编译期固定容量模型 → boot 期堆分配器未建立 → Rust [T; N] 自然表达；NR_* 常量关系）  ← 新增
│  ├─ §2.1 进程表存储（C 侧：proc[] 静态数组 + proc_addr 宏索引 + 字段分组导航）
│  │  ├─ §2.1.0 整体：proc[NR_TASKS+NR_PROCS] 静态数组、proc_addr(n) O(1) 定位
│  │  ├─ §2.1.1 标识组 → 17
│  │  ├─ §2.1.2 调度属性组 → 11
│  │  ├─ §2.1.3 IPC 状态组 → 12
│  │  ├─ §2.1.4 执行上下文组 → 10/09
│  │  ├─ §2.1.5 记账/统计组（导航归类，不展开）→ 17/11
│  │  └─ §2.1.6 生命周期控制组（RTS 16 位全集）→ 11
│  ├─ §2.2 特权表存储
│  │  ├─ §2.2.0 整体：priv[64] + ppriv_addr[NR_PROCS] + USER_PRIV 分治（纯 C 侧）
│  │  ├─ §2.2.1 身份/能力组 → 22
│  │  ├─ §2.2.2 信号组 → 19
│  │  ├─ §2.2.3 IPC 组 → 22/23
│  │  ├─ §2.2.4 I/O 组 → 20
│  │  ├─ §2.2.5 内存组 → 24
│  │  └─ §2.2.6 运行时组 → 21/17
│  └─ §2.3 boot image 存储（精简版）
├─ Ch3 Rust 设计决策（按分组重表达，~150 行）     ← 与 Ch2 分组概念对应（仅独立决策建节，其余汇总表带过）
│  ├─ §3.0 核心设计原则（三层因果链/BKL 并发协议/SMP 原子性）
│  ├─ §3.1 进程表设计（与 §2.1 概念对应：固定数组、指针→索引、Endpoint/ProcNr newtype）
│  ├─ §3.2 特权表设计（与 §2.2 概念对应：PrivTable 固定数组、CapabilityTemplate 枚举）
│  ├─ §3.3 RTS 设计（bitflags + AtomicU32 + 不变量强制；论据 = 真实并发协议，见 §6.3）
│  ├─ §3.4 boot image 类型设计（ProcKind + EntrySpec）
│  ├─ §3.5 过渡到启动主线：CpuContextArch trait（保留，压缩）
│  └─ §3.6 设计决策汇总表（v2.1 修订：原 §3.15 重编号——grep 确认无外部文档锚点引用 §3.15，消除 3.5→3.15 跳号；含「无独立决策分组」的对照行）
├─ Ch4 Rust 实现详解：阶段 C 启动主线（~150 行）  ← 启动主线的实现落点，节拍不得砍薄（A15）
│  ├─ §4.0 实现地图：init_proc_and_boot() 主流程（保留）
│  ├─ §4.1–§4.6 arch 层/kernel 层实现（压缩，与 Ch3 对齐）
│  ├─ §4.7 主流程 init_proc_and_boot()（保留完整：清空→填充→加载→唤醒节拍在此落地）
│  └─ §4.8 bsp_finish_booting（保留，细节指向 08）
├─ Ch5 测试要点 (~50–80 行；§5.0–5.3 + 覆盖矩阵保留，§5.4 review 产物移出正文)
└─ Ch6 参见（扩充 11/12/17/22/19/20/21/23/24；写入 §6.1b 职责边界矩阵）
```

---

## 8. 分步执行工作项

> 建议按依赖顺序执行。每步完成后保留「§9 验收」中对应的证据。

**执行总注意（GPT 终审提醒，执行级非 spec 缺陷）**：
1. **不要把「引用已有文档」机械化**——「属于 scheduler → 11」不等于只写「详见 11」。每组必须保留「**为什么阶段 C 此时需要这个状态**」的因果链：`p_rts_flags → 阶段 C 初始化为什么必须设置它 → 为什么这个 proc 能/不能进入 runnable → 运行时如何维护 → 详见 11`，**最后一层才下放**。否则 06 会退化成一张目录（这违反 A15 与 §10.9 的质量标准）。
2. **不要为凑 800–1000 行主动压缩**——让文档自然长出来；1300 行但结构好完全接受（A1 已明确行数非验收指标）。

### Step 0：前置修复（v2 新增——执行 06 改造的依赖，必须先完成）

1. **修 22-privilege.md 的 KPriv 子结构计数**：22 §4.2 与其 Ch3 D1 写「6 子结构」，但代码实际为 **8 个**（[os/kernel/src/kpriv.rs](file:///os/kernel/src/kpriv.rs)：`PrivIdentity`/`PrivInit`/`PrivFlags`/`PrivSignals`/`PrivIpc`/`PrivIo`/`PrivMem`/`PrivRuntime`，**无 `PrivIrq`**）；06 §3.10 标题写「8」但表格列了 9 行（多写 `PrivIrq`）。**以 kpriv.rs 为准统一为 8**，先改 22 再改 06——否则 Step 5 的「06 → 22 引用」会指向错误内容。
2. **建立外部引用清单**：`rg "06 §|proc-init-boot-proc\.md §|proc-init-boot-proc\.md#L" notes/` 清点全仓库对 06 的显式锚点引用，作为重构输入与 A9b 验收基线。已知实例：`03-stage-rs/07-rs-period-heartbeat.md` 引用「06 §2.1/§2.2/§2.6」；`00-kernel-overview.md` 引用「06 §1.1.4」；`12-ipc-core.md`/`13-syscall-dispatch.md` 把 06 当「struct proc 字段语义」权威——重构后该职责转移，这两处需**重定向**（语义归属变化，非编号问题）。

### Step 1：建立全文映射表（不改文档，只审计）

产出：一张「现有内容 → 内容类型 → 处置（保留/精简/删除/移到哪）→ 目标章节 → 原因 → 下游承接」的映射表，作为执行依据。同时 grep 确认后续文档（11/12/17/22/19/20/21/23/24）**确实已覆盖** 06 拟下放的运行时内容（见第 11 节证据），防止下放后出现缺口。

**映射表必须为每个现有小节给出明确去向，且必须包含「内容类型 / 原因 / 下游承接」三列**（v2.1 修订，GPT 复审：不仅「去哪里」，还要「为什么去那里」——未来据此判断内容是「真正转移」还是「丢失」）。特别是以下未定项：

| 现有小节 | 内容类型 | 去向 | 原因 | 下游承接 |
|---------|---------|------|------|---------|
| §3.6 FPU 架构演进 | Rust 设计（专题） | → 31-fpu-context-switching.md | 独立概念，与启动主线无关（概念边界，A1） | 先确认 31 覆盖「不翻译 fnsave」论证，否则 Ch3 保留一小节 |
| §3.9 SMP 预留设计（BKL+Atomic 论证） | Rust 设计（并发协议） | → 11-scheduling-primitives.md，或在 §3.0 设计原则留一段 | 并发协议属调度域 | 11 §1.1 覆盖不变量；§3.0 保留协议论据 |
| §3.13 错误处理与不变量表达 | Rust 设计（横切） | → 散入 §3.0 与各分组对应节 | 横切关注点，无独立小节价值 | 各组「与 C 的差异」行 |
| §3.14 fork 对照 | 运行时语义 | → 17 §1.1（删除 06 内重复） | 17 已覆盖，重复讲述 | ✅ 17 §1.1 |
| §5.4 L3 grep 证据块 | review 产物 | → `.review/`（移出正文） | 流程性内容，不符教学口吻（A11） | — |

### Step 2：新增 §2.0 内核存储约束总述

写清**三层因果链**（v2 修订，见 §10.5）+ `NR_TASKS`/`NR_PROCS`/`NR_SYS_PROCS`/`NR_BOOT_PROCS`/`NR_BOOT_MODULES` 的常量关系（见 §4.3 对照表）。这是「内核必须讲怎么存」的总纲。不得写成「no_std 所以必须静态数组」的单一口号。

### Step 3：重写 §1.3 三件套（概念层）

当前 §1.3（约 105 行，含大量字段细节）压缩为每件 ~30 行的模型 + 引用。删掉字段细节（它们属于 Ch2 分组）。

### Step 4：按 §6 分组重构 Ch2（最大工作量）

把现有 Ch2 的逐函数分析重组为「分组」结构。**不要整段复制旧 Ch2**——按「Ch2 模板」（§6.1 示例：字段 → OS 语义 → C 结构关系 → 阶段 C 作用 → 引用）逐组重写。原有函数级分析（`proc_init` 循环细节、`get_priv` 分支、`arch_boot_proc` 等）压缩为分组内的一句话 + 代码锚点，或移入 Ch4 的对应步骤。**Ch2 组内禁止出现 Rust 类型**（newtype/Atomic/VecDeque 等，验收 A3）。

### Step 5：重写 Ch3 为「按分组的 Rust 设计决策」

对**存在独立 Rust 设计决策**的分组按「Ch3 模板」（C 结构 → Rust 表达 → 为什么 → 与 C 差异）建立小节，每组 ~30 行；与 Ch2 **概念对应**（非机械镜像——无独立决策的组在 §3.6 汇总表一行带过，不强行建节，见 §6.1 规则 6）。删除与 22 重复的 KPriv 详细字段表（只留索引 + 引用）。**前置依赖：Step 0 已完成 22 的 KPriv 计数修正（8 子结构）**，否则引用指向错误内容。

### Step 6：精简 Ch1 §1.4 / §1.6 / 附录 A

- §1.4 boot image：压到 ~60 行（概念 + 编号表 + multiboot 关系一句）。
- §1.6 唤醒：动作列表压缩，细节指向 08。
- 附录 A libexec：删除或压缩为一段（Rust 用 `minix_elf`，C 框架仅作对照说明）。

### Step 7：更新 Ch6 参见 + 全文交叉引用

- 扩充 `参见` 到 11/12/17/22/19/20/21/23/24。
- 核对 06 内所有「详见 XX §Y」引用是否指向正确目标章节（后续文档的真实小节号）。

### Step 8：检查下游引用完整性（v2 修订：文件级 + 锚点级）

1. **文件级**：grep 全仓库对 `06-proc-init-boot-proc.md` 的引用（尤其 07/08/10/12/17/11/22），确认链接能解析（A9a）。
2. **锚点级**：对照 Step 0 的外部引用清单，逐一核对 `06 §X.Y` 显式引用——编号漂移处同步修正下游文档；「字段语义权威」类引用（12/13）重定向到新权威（A9b）。
3. Design-First 流程：§7 outline 修正后先落成 `.design/06-outline.v2.md`（review 过）再动正文，避免「边改边定结构」。

### Step 9：测试章节核对 + 更新

Ch5 测试矩阵与真实代码 `rg "#\[test\]"` 核对，保留代表性测试；如测试有新增/改名，同步更新。

---

## 9. 验收标准

> 每个标准应可通过 grep / 阅读独立验证。标「✅/❌」逐项完成。

| # | 验收项 | 验证方法 |
|---|--------|---------|
| A1 | **概念边界**（v2.1 修订）：06 只承载「阶段 C 启动主线 + 其存储模型」一个核心概念——FPU→31、fork→17、libexec→删、VM 协议→09、ELF 细节→02-stage-vm 均已下放/删除。总行数为参考指标（预期 ~800–1000，**不作为 pass/fail**——单一难概念可承载更长篇幅）；若超 1200 行，交付时须逐节说明新增内容的必要性 | 概念清单核对 + `wc -l` |
| A2 | Ch2 存在 §2.0 存储约束总述，含**三层因果链**（Minix 编译期固定容量模型 → boot 期堆分配器未建立 → Rust `[T; N]` 自然表达），无「no_std 所以必须静态数组」式口号 | grep `## 2.0` + 阅读 |
| A3 | §2.1 进程表按 ≥5 个分组组织，每组有「OS 语义」一句话；**Ch2 组内无 Rust 类型**（newtype/Atomic/VecDeque 等） | 阅读 §2.1 + grep 抽查 |
| A4 | §2.2 特权表按 ≥6 个分组组织（与 22 一致的 6 组），含存储策略（分治/双表/零堆） | 阅读 §2.2 |
| A5 | RTS 16 位全集在 06 中可 grep 到，不变量口号存在 | grep `runnable iff` / 16 个位名 |
| A6 | 每个分组末尾有指向 11/12/17/22 等的「详见」引用，且目标文件存在 | grep `](XX-...)` + 文件存在 |
| A7 | 附录 A libexec 已删除或压缩（≤ 一段） | grep `附录 A` |
| A8 | 全文不再出现「逐函数展开的 arch_boot_proc/libexec 回调细节」（已下放/压缩） | 阅读 |
| A9a | 下游文档对 06 的**文件级**引用仍有效（链接能解析） | grep 全仓库 + 文件存在 |
| A9b | 下游对 06 的**锚点级**引用逐一核对（`rg "06 §\|proc-init-boot-proc\.md §\|proc-init-boot-proc\.md#L"` 对照 Step 0 清单），编号漂移处已同步修正、语义归属变化处已重定向 | grep + 逐条核对表 |
| A10 | Ch5 测试矩阵与 `rg "#\[test\]"` 实际测试核对通过（无虚构测试名） | grep |
| A11 | 06 中不再出现「已实现/待实现/未实现/WIP/🚧」等进度式表述（保持教学文档口吻）；§5.4 review 产物已移出正文 | grep |
| A12 | Ch2 每组符合「字段→OS 语义→C 结构关系→阶段 C 作用→引用」模板；Ch3 独立小节符合「C 结构→Rust 表达→为什么→与 C 差异」模板；Ch2/Ch3 **概念对应**（无独立决策的组在汇总表带过，不强造小节）；每组遵守 §6.1 规则 5（OS 语义不展开成运行时协议） | 抽样阅读 3 组 |
| A13 | **反重复**：对 11/12/17/22 已负责的运行时行为（enqueue/dequeue、IPC 状态机、fork 修改字段、capability 语义），06 只说明其在阶段 C 中的存在/初始化作用并引用转交，**不重复建立运行时状态机** | 抽样阅读 + grep 运行时关键词（enqueue 后续步骤/fork 步骤等） |
| A14 | **反百科**：非阶段 C 必需字段（记账/统计等）只归类导航、不逐字段展开；字段全集以导航表形式存在（可全列）；每组末尾有转交引用 | 阅读 + 抽样 |
| A15 | **启动主线完整性**：Ch4 保留 清空→填充→加载→唤醒 完整节拍与 bsp_finish_booting 主流程，§1.5 执行节拍保留——未因分组改造被砍薄 | 阅读 §1.5/§4.0/§4.7/§4.8 |

**验收线**：A1（概念边界）、A2–A4、A6、A9a、A9b、A13、A15 为硬性（必须全过）；其余建议达成。（v2.1 注：A1 的硬性部分是概念边界，行数仅触发「超 1200 行需说明」的义务，不单独判定失败。）全部通过后 06 视为重构完成。

---

## 10. 约束与注意事项

1. **ground truth 优先**：字段名、容量、数值以 `minix3/minix/kernel/proc.h`、`priv.h`、`param.h`、`com.h` 为准，不得凭记忆写。行为不确定的标注「待确认」。
2. **文档不得出现进度式表述**（已实现/待实现/✅❌🚧）——教学文档只描述系统行为（§2.11 风格约束）。
3. **引用必须是后续文档真实存在的小节**：先确认目标小节号再写「详见」。后续文档引用以相对路径（`NN-xxx.md`）格式，不写绝对路径。
4. **保持阶段 C 锚点地位**：06 是 07/08/10/12 等文档引用的「阶段 C」入口。章节编号（Ch1/Ch2…换层后）可调整，但文档定位不变——它是「Stage C 主线 + 存储支撑」，不是「字段百科」。
5. **存储因果链必须三层展开**（v2 修订）：第一层，Minix 模型本身是编译期固定容量（`NR_PROCS`/`NR_SYS_PROCS` 是常量）；第二层，kernel **boot 阶段堆分配器尚未建立**（注意 `no_std` ≠ 无 allocator——是 boot 期未建立，不得写「no_std 所以必须静态数组」）；第三层，因此 Rust 表达 `[KProcess; N]` / `[KPriv; 64]` 是自然选择。所有「为什么这么存」的论证落到这三层，不得写成用户态可用的泛泛理由。
6. **SMP/BKL**：进程表、RTS 位图涉及多 CPU 时，讲清 `Atomic`/BKL 保护；禁止在跨 CPU 场景使用 `Rc`/`RefCell` 的说法。**并发协议论据以真实代码为准**（v2 修订）：BKL 串行化所有写；Atomic 支撑跨 CPU 无锁读（如调度扫描 `load(Relaxed)`）。`p_nextready: AtomicI32`、`RtsFlags: AtomicU32` 是**现存已验证设计**（proc_table.rs/sched.rs），可直接写，但「为什么」必须落到真实访问协议，不得写成「SMP → 所以 Atomic」的口号。
7. **不新增文档**：本次重构**不新建** 06a/06b/… 文档；所有内容收敛在 06 单文档内，或下放**到已有**的后续文档。
8. **文风**：中文，面向「理解 Minix3 微内核基本结构」的开发者；图表必要才用；代码块注释用中文。
9. **主线与存储并重**（v2 新增）：本次改造的重心是「分组 + 下放」，但**不得把阶段 C 启动主线砍薄**——Ch1 §1.5 执行节拍与 Ch4 的 清空→填充→加载→唤醒 主流程必须完整保留（验收 A15）；同时不得让字段分组喧宾夺主（验收 A14）。06 的最终质量标准：**读者能从 06 独立回答「阶段 C 做什么、为什么需要 proc/priv/RTS、这些结构怎么存、boot image 如何进入 proc table、最终如何形成第一个可运行环境」**。

---

## 11. 附录：后续文档内容证据

> 下表证明「后续文档已覆盖 06 拟下放的运行时内容」，是「不拆文档、只下放」决策的证据。已用 grep 验证。

| 后续文档 | 已覆盖内容（grep 命中示例） | 结论 |
|---------|---------------------------|------|
| `11-scheduling-primitives.md` | §1.1-1.3：RTS 不变量 `p_rts_flags == 0 ⟺ runnable`、enqueue/dequeue 联动、16 级队列；Rust 源码 `os/kernel/src/sched.rs` | ✅ 可承接 RTS 运行时细节 |
| `12-ipc-core.md` | §1.2 六原语与 RTS 标志变化、§1.3 delivermsg 延迟拷贝、§1.4 RECEIVE 优先级 | ✅ 可承接 IPC 字段用法 |
| `17-syscall-process.md` | §1.1 生命周期弧、§1.2 同步 fork（`RTS_ISSET(rpp, RTS_RECEIVING)`）、endpoint 代际递增（`_ENDPOINT_G`/`fork_new_endpoint`）、§1.3 权限降级（`RTS_NO_PRIV`） | ✅ 可承接进程表字段运行时读写 |
| `22-privilege.md` | §1.1 31 字段 6 组、§1.2 s_flags 位表、§1.3 三类掩码、`priv[NR_SYS_PROCS=64]`、`ppriv_addr[]`、设计决策 D1-D6（含 PrivTable 固定数组） | ✅ 可承接特权表字段语义 + CapabilityTemplate；06 只保留「存储策略」总述，详细字段语义引用 22 |
| `19/20/21/23/24-*.md` | 信号（19）、设备 I/O（20）、时钟/闹钟（21）、IPC 过滤（23）、跨空间（24） | ✅ 可承接 priv 各分组运行时用法 |

**注意**：22 的 §1.1 已经讲完 struct priv 31 字段分组，06 §2.2 不应重复展开「每个字段含义」，只讲「存储形态（分治/双表/零堆）+ 分组索引 + 引用 22」。这是避免 06 与 22 重复的关键边界。

**已知不一致（v2 记录，Step 0 修复）**：KPriv 子结构计数三方不一致——06 §3.10 标题「8」但表格 9 行（多写 `PrivIrq`）、22 §4.2 写「6」、[kpriv.rs](file:///os/kernel/src/kpriv.rs) 实际 **8 个**（无 `PrivIrq`）。以代码为准统一为 8，先修 22 再执行 06 改造（Step 0）。

---

## 附：本需求的来源对话摘要（供审阅者理解前因）

1. 初始反馈：「06 又大、又讲得不好」；观察 `02-stage-vm/` 的拆分模式（vmproc struct/table/acl 各一篇），考虑拆 5 篇。
2. 深化：「01-stage-kernel 整体结构加入考虑后，也许不需要拆 5 篇，而是把内容挪入后续文档」——因为 11/12/17/22 等已存在且覆盖运行时内容。
3. 纠偏（本轮核心）：「06 至少需要讲清楚进程表具体如何存储；`[]struct 就是 table`，内核 no_std 下必须讲清怎么存（不像用户态 `Vec::new()`）」「字段应分组，讲每组完成的 OS 语义，细节归属后续文档；priv 同理」。
4. 收敛：**不拆、不新建文档**；06 保留为「进程表/特权表/RTS 存储布局 + 阶段 C 启动主线」单文档，按分组组织，运行时细节引用后续文档，目标 2192 → ~800 行。
5. GLM review（2026-08-29，v2 采纳）：章节契约违反（Ch2 不得改名「存储布局」、模板不得把 Rust 设计泄入 Ch2）→ §6/§7/Step 4 修订；A9 太弱（显式 § 锚点引用会断：03-stage-rs/07 引「06 §2.1/2.2/2.6」、00-overview 引「§1.1.4」、12/13 引用语义归属变化）→ A9a/A9b + Step 0 外部引用清单；KPriv 8/9/6 三方不一致 → Step 0 先修 22；Step 1 映射表缺去向（§3.6/3.9/3.13/3.14/5.4）→ Step 1 表。
6. GPT review（AI-chats/comments.md，2026-08-29，v2 采纳）：「字段分组=导航不是覆盖」→ §6.1 四条规则 + A13/A14；职责边界矩阵 → §6.1b；`no_std ≠ 无 allocator` + 三层因果链 → §10.5/Step 2；06 使命句重写（Stage C 主线、storage 支撑）→ §3.3/§4.4；行数硬指标的坏优化风险 → §5/§9 A1（保留 ≤1000 硬上限，~800 降为参考）。
7. 用户校准（2026-08-29，v2 采纳）：**启动主线与存储布局并重**——不得只强调存储而砍薄启动节拍（新增 A15、§10.9）；Atomic 等设计为「现存已验证」（proc_table.rs/sched.rs），论据落到真实并发协议（BKL 串行化写 + Relaxed 无锁读）而非预设口号；字段全集保留导航表（用户原话「可列举所有字段」），展开仅限阶段 C 触碰字段。
8. GPT 复审 + 用户再校准（2026-08-29，v2.1 采纳）：
   - GPT P0-1「OS 语义不得膨胀成运行时协议」→ §6.1 规则 5 + A12；
   - GPT P0-2「Ch3 镜像 → 概念对应」（防为凑镜像强行写无价值 Rust 设计小节）→ §6.1 规则 6、§5 表、§7、Step 5、A12；
   - GPT P1「§3.15 → §3.6 重编号」→ §7（grep 确认无外部文档锚点引用 §3.15；`20-syscall-device.md` 引用的 §3.5 不受影响）；
   - GPT P1「Step 1 映射表加 内容类型/原因/下游承接 三列」→ Step 1；
   - **用户再校准（v2.1 核心判断修正）**：行数不是问题——单一难概念 2000+ 行可接受，06 的病根是**混入太多概念** → §3.2 问题 1 重新诊断（行数是症状、概念混杂是病根）、A1 从「≤1000 硬上限」改为「概念边界」验收（行数仅参考，超 1200 需逐节说明必要性）。
   - GPT 评级：可执行，~90–95% ready；下一步进入执行（Step 0 → outline.v2 → 正文 → A1–A15 验收）。
9. GPT 终审（2026-08-29）：**spec 定稿，开始执行**——「不要再继续做设计层面的迭代」。执行要求：①**三阶段顺序**——第一阶段不碰 06 正文（Step 0 前置修复 → Step 1 映射表【最重要中间产物】→ `.design/06-outline.v2.md`）→ 停下 review outline → 第二阶段才改正文 → 第三阶段按 A1–A15 验收（重点 A1/A2–A4/A6/A9a/A9b/A13/A15）；②执行总注意见 §8 开头（引用不机械化、不凑行数压缩）。下一轮 review 价值点：验证重构后**概念边界是否真的变清楚**。