# 08-grep 与 sed：正则语义面

> **状态**: pending（最小骨架，待改写）
> **定位**: grep 与 sed：正则语义面
> **源码**: `minix3/minix/usr.bin/grep/、usr.bin/sed/`
> **Rust 模块**: `新 crate（待建）`
> **draft 素材**: 无（新建）

## 核心点

- - grep 族：minix/usr.bin/grep 行为（选项 -E/-F/-r/-c/-l/-n 等、退出码 0/1/2）
- - sed 流编辑：地址/命令面（s/d/p/a/i/c/y）、-n/-e/-f 选项、脚本文件面
- - 正则语法契约：BRE/ERE（基础/扩展正则、锚定、字符类、后向引用）
- - 正则引擎语义：贪婪匹配、空匹配推进、多行面
- - 与 09（ed 正则）的共享语法面

## 边界

- - **前置依赖**: 07
- - **不覆盖（移交）**: 编辑器中的正则使用（09）、文本过滤命令（07）

