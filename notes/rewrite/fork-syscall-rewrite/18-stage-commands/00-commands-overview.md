# 00-总览：命令面全图与交付链路

> **状态**: pending（最小骨架，待改写）
> **定位**: 总览：命令面全图与交付链路
> **源码**: `minix3/bin/、sbin/、usr.bin/、usr.sbin/、minix/commands/、minix/usr.bin/、games/、etc/、libexec/getty/`
> **Rust 模块**: `os/commands/*（35 crate）、os/etc/`
> **draft 素材**: 无（新建）

## 核心点

- - 用户系统交付因果链总图：boot → init（01）→ rc → 服务（02）→ 登录（03）→ 设备/数据库（04）→ shell（05）→ 命令使用（06~24）
- - 命令面全图：328 命令 / 865 .c / 425,130 行（§5.1 实测统计），7 个 C 源目录 + getty/login
- - 功能域矩阵：文件(06)/文本(07)/正则(08)/编辑(09)/文档(10)/归档(11)/进程(12)/终端(13)/存储(14~17)/网络(18/19)/系统(20/21)/游戏(22~24)
- - 文档导航：26 篇（00 + 24 + 99），编号 = 交付链阅读顺序；与实施顺序（§6）解耦
- - 设计原则：非 server 主线重定义（参照 14/15/16/17 先例）；位置可回答性 + 功能域归属

## 边界

- - **前置依赖**: 01-stage-kernel（引导完成）、14-stage-runtime（exec/stdio/termios）、17-stage-net（socket）
- - **不覆盖（移交）**: 一切命令机制细节（01~99 各篇）

