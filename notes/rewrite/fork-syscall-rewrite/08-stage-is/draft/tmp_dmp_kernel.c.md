# IS Server - dmp_kernel.c 逐行讲解

## 文件概述

**文件路径**: `minix/servers/is/dmp_kernel.c`

**核心功能**: 内核数据结构的调试转储函数，包括进程表、权限表、系统映像、IRQ表、内核消息等。

**设计思路**: 通过系统调用从内核获取数据副本，然后在用户态格式化输出。遵循微内核原则：用户态服务不直接访问内核内存。

---

## 头文件包含

```c
/* Debugging dump procedures for the kernel. */

#include "inc.h"
#include <lib.h>
#include <minix/timers.h>
#include <assert.h>
#include <machine/interrupt.h>
#include <minix/endpoint.h>
#include <minix/sysutil.h>
#include <minix/sys_config.h>
#include "kernel/const.h"
#include "kernel/config.h"
#include "kernel/debug.h"
#include "kernel/type.h"
#include "kernel/proc.h"
#include "kernel/ipc.h"
```

**逐行讲解**:
- 包含内核相关头文件
- 注意：包含`kernel/proc.h`等内核头文件
- 这些头文件定义了内核数据结构
- IS使用这些定义来解析从内核获取的数据

---

## 常量定义

```c
#define LINES 22
```

**逐行讲解**:
- `LINES 22`：每页显示的行数
- 用于分页显示大量数据

---

## 宏定义

```c
#define PRINTRTS(rp) { \
	char *procname = "";	\
	printf(" %s", p_rts_flags_str(rp->p_rts_flags));	\
	if (rp->p_rts_flags & RTS_SENDING)				\
		procname = proc_name(_ENDPOINT_P(rp->p_sendto_e)); \
	else if (rp->p_rts_flags & RTS_RECEIVING)			\
		procname = proc_name(_ENDPOINT_P(rp->p_getfrom_e)); \
	printf(" %-7.7s", procname);	\
}
```

**逐行讲解**:
- `PRINTRTS(rp)`：打印进程运行时标志
  - 参数`rp`：进程结构指针
- `p_rts_flags`：运行时状态标志
  - `RTS_SENDING`：正在发送消息
  - `RTS_RECEIVING`：正在接收消息
- 根据状态打印目标进程名

---

```c
#define PROCLOOP(rp, oldrp) \
	pagelines = 0; \
	for (rp = oldrp; rp < END_PROC_ADDR; rp++) { \
	  oldrp = BEG_PROC_ADDR; \
	  if (isemptyp(rp)) continue; \
	  if (++pagelines >= LINES) { oldrp = rp; printf("--more--\n"); break; }\
	  if (proc_nr(rp) == IDLE) 	printf("(%2d) ", proc_nr(rp));  \
	  else if (proc_nr(rp) < 0) 	printf("[%2d] ", proc_nr(rp)); 	\
	  else 				printf(" %2d  ", proc_nr(rp));
```

**逐行讲解**:
- `PROCLOOP(rp, oldrp)`：遍历进程表的宏
  - 支持分页显示
  - 跳过空槽
  - 格式化进程号显示
    - `()`：IDLE进程
    - `[]`：系统任务
    - 普通：用户进程

---

```c
#define click_to_round_k(n) \
	((unsigned) ((((unsigned long) (n) << CLICK_SHIFT) + 512) / 1024))
```

**逐行讲解**:
- `click_to_round_k(n)`：将click转换为KB
  - `CLICK_SHIFT`：click大小（通常为12，即4KB）
  - 加512后除1024实现四舍五入

---

## 全局数据

```c
static int pagelines;

/* Declare some local dump procedures. */
static char *proc_name(int proc_nr);
static char *s_traps_str(int flags);
static char *s_flags_str(int flags);
static char *p_rts_flags_str(int flags);

/* Some global data that is shared among several dumping procedures.
 * Note that the process table copy has the same name as in the kernel
 * so that most macros and definitions from proc.h also apply here.
 */
struct proc proc[NR_TASKS + NR_PROCS];
struct priv priv[NR_SYS_PROCS];
struct boot_image image[NR_BOOT_PROCS];
```

**逐行讲解**:
- `struct proc proc[NR_TASKS + NR_PROCS]`：进程表副本
  - 与内核中的进程表同名，便于使用内核宏
- `struct priv priv[NR_SYS_PROCS]`：权限表副本
- `struct boot_image image[NR_BOOT_PROCS]`：启动映像副本

**内存布局**:
```
静态数据段:
┌────────────────────────────────────────┐
│ proc[NR_TASKS+NR_PROCS] │ 进程表副本   │
│ priv[NR_SYS_PROCS]      │ 权限表副本   │
│ image[NR_BOOT_PROCS]    │ 启动映像副本 │
└────────────────────────────────────────┘
```

---

## kmessages_dmp函数

```c
/*===========================================================================*
 *				kmessages_dmp				     *
 *===========================================================================*/
void
kmessages_dmp(void)
{
  struct kmessages *kmess;		/* get copy of kernel messages */
  static char print_buf[_KMESS_BUF_SIZE+1]; /* this one is used to print */
  int start;				/* calculate start of messages */
  int r;
  int size;

  kmess = get_minix_kerninfo()->kmessages;
```

**逐行讲解**:
- `void kmessages_dmp(void)`：显示内核消息
  - 按F7触发
- `get_minix_kerninfo()->kmessages`：获取内核消息缓冲区指针
  - 这是直接访问内核共享内存区域

---

```c
  /* Try to print the kernel messages. First determine start and copy the
   * buffer into a print-buffer. This is done because the messages in the
   * copy may wrap (the kernel buffer is circular).
   */
  start = ((kmess->km_next + _KMESS_BUF_SIZE) - kmess->km_size) % _KMESS_BUF_SIZE;
  r = 0;
  size = kmess->km_size;
  while (size > 0) {
  	print_buf[r] = kmess->km_buf[(start+r) % _KMESS_BUF_SIZE];
  	r ++;
  	size--;
  }
  print_buf[r] = 0;		/* make sure it terminates */
  printf("Dump of all messages generated by the kernel.\n\n");
  printf("%s", print_buf);		/* print the messages */
}
```

**逐行讲解**:
- 内核消息缓冲区是环形缓冲区
- 计算起始位置
- 处理环绕情况
- 复制到线性缓冲区
- 打印消息

**环形缓冲区处理**:
```
km_buf[_KMESS_BUF_SIZE] (环形):
┌───┬───┬───┬───┬───┬───┬───┬───┐
│ A │ B │ C │ D │ E │   │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
  ↑                   ↑
  start               km_next

如果缓冲区满，新消息覆盖旧消息
需要正确处理环绕
```

---

## monparams_dmp函数

```c
/*===========================================================================*
 *				monparams_dmp				     *
 *===========================================================================*/
void
monparams_dmp(void)
{
  char val[MULTIBOOT_PARAM_BUF_SIZE];
  char *e;
  int r;

  /* Try to get a copy of the boot monitor parameters. */
  if ((r = sys_getmonparams(val, sizeof(val))) != OK) {
      printf("IS: warning: couldn't get copy of monitor params: %d\n", r);
      return;
  }

  /* Append new lines to the result. */
  e = val;
  do {
	e += strlen(e);
	*e++ = '\n';
  } while (*e != 0);

  /* Finally, print the result. */
  printf("Dump of kernel environment strings set by boot monitor.\n");
  printf("\n%s\n", val);
}
```

**逐行讲解**:
- `void monparams_dmp(void)`：显示启动监视器参数
  - 按F5触发
- `sys_getmonparams(val, sizeof(val))`：从内核获取启动参数
- 参数是字符串数组，以空字符分隔
- 添加换行符后打印

---

## irqtab_dmp函数

```c
/*===========================================================================*
 *				irqtab_dmp				     *
 *===========================================================================*/
void
irqtab_dmp(void)
{
  int i,r;
  struct irq_hook irq_hooks[NR_IRQ_HOOKS];
  int irq_actids[NR_IRQ_VECTORS];
  struct irq_hook *e;	/* irq tab entry */

  if ((r = sys_getirqhooks(irq_hooks)) != OK) {
      printf("IS: warning: couldn't get copy of irq hooks: %d\n", r);
      return;
  }
  if ((r = sys_getirqactids(irq_actids)) != OK) {
      printf("IS: warning: couldn't get copy of irq mask: %d\n", r);
      return;
  }
```

**逐行讲解**:
- `void irqtab_dmp(void)`：显示IRQ钩子表
  - 按F6触发
- `sys_getirqhooks(irq_hooks)`：获取IRQ钩子数组
- `sys_getirqactids(irq_actids)`：获取IRQ活动掩码

---

```c
  printf("IRQ policies dump shows use of kernel's IRQ hooks.\n");
  printf("-h.id- -proc.nr- -irq nr- -policy- -notify id- -masked-\n");
  for (i=0; i<NR_IRQ_HOOKS; i++) {
  	e = &irq_hooks[i];
  	printf("%3d", i);
  	if (e->proc_nr_e==NONE) {
  	    printf("    <unused>\n");
  	    continue;
  	}
  	printf("%10d  ", e->proc_nr_e);
  	printf("    (%02d) ", e->irq);
  	printf("  %s", (e->policy & IRQ_REENABLE) ? "reenable" : "    -   ");
  	printf("   %4lu", e->notify_id);
	if (irq_actids[e->irq] & e->id)
		printf("       masked");
	printf("\n");
  }
  printf("\n");
}
```

**逐行讲解**:
- 遍历所有IRQ钩子
- 打印每个钩子的信息：
  - ID
  - 进程端点
  - IRQ号
  - 策略（是否自动重启用）
  - 通知ID
  - 是否被屏蔽

**IRQ钩子结构**:
```
struct irq_hook {
    endpoint_t proc_nr_e;  // 处理该IRQ的进程
    int irq;               // IRQ号
    int policy;            // 策略标志
    unsigned long notify_id; // 通知ID
    unsigned long id;      // 钩子ID
};
```

---

## image_dmp函数

```c
/*===========================================================================*
 *				image_dmp				     *
 *===========================================================================*/
void
image_dmp(void)
{
  int m, r;
  struct boot_image *ip;

  if ((r = sys_getimage(image)) != OK) {
      printf("IS: warning: couldn't get copy of image table: %d\n", r);
      return;
  }
  printf("Image table dump showing all processes included in system image.\n");
  printf("---name- -nr- flags -stack-\n");
  for (m=0; m<NR_BOOT_PROCS; m++) {
      ip = &image[m];
      printf("%8s %4d\n", ip->proc_name, ip->proc_nr);
  }
  printf("\n");
}
```

**逐行讲解**:
- `void image_dmp(void)`：显示系统映像表
  - 按F3触发
- `sys_getimage(image)`：获取启动映像表
- 打印每个启动进程的信息

---

## kenv_dmp函数

```c
/*===========================================================================*
 *				kenv_dmp				     *
 *===========================================================================*/
void
kenv_dmp(void)
{
    struct kinfo kinfo;
    struct machine machine;
    int r;
    if ((r = sys_getkinfo(&kinfo)) != OK) {
    	printf("IS: warning: couldn't get copy of kernel info struct: %d\n", r);
    	return;
    }
    if ((r = sys_getmachine(&machine)) != OK) {
    	printf("IS: warning: couldn't get copy of kernel machine struct: %d\n", r);
    	return;
    }

    printf("Dump of kinfo structure.\n\n");
    printf("Kernel info structure:\n");
    printf("- nr_procs:     %3u\n", kinfo.nr_procs);
    printf("- nr_tasks:     %3u\n", kinfo.nr_tasks);
    printf("- release:      %.6s\n", kinfo.release);
    printf("- version:      %.6s\n", kinfo.version);
    printf("\n");
}
```

**逐行讲解**:
- `void kenv_dmp(void)`：显示内核信息
  - 按F10触发
- `sys_getkinfo(&kinfo)`：获取内核信息结构
- `sys_getmachine(&machine)`：获取机器信息
- 打印进程数、任务数、版本等信息

---

## s_flags_str函数

```c
/*===========================================================================*
 *			      s_flags_str				     *
 *===========================================================================*/
static char *s_flags_str(int flags)
{
	static char str[10];
	str[0] = (flags & PREEMPTIBLE)        ? 'P' : '-';
	str[1] = (flags & BILLABLE)           ? 'B' : '-';
	str[2] = (flags & DYN_PRIV_ID)        ? 'D' : '-';
	str[3] = (flags & SYS_PROC)           ? 'S' : '-';
	str[4] = (flags & CHECK_IO_PORT)      ? 'I' : '-';
	str[5] = (flags & CHECK_IRQ)          ? 'Q' : '-';
	str[6] = (flags & CHECK_MEM)          ? 'M' : '-';
	str[7] = '\0';

	return str;
}
```

**逐行讲解**:
- `static char *s_flags_str(int flags)`：将权限标志转换为字符串
  - P：可抢占
  - B：可计费
  - D：动态权限ID
  - S：系统进程
  - I：检查IO端口
  - Q：检查IRQ
  - M：检查内存

---

## s_traps_str函数

```c
/*===========================================================================*
 *			      s_traps_str				     *
 *===========================================================================*/
static char *s_traps_str(int flags)
{
	static char str[10];
	str[0] = (flags & (1 << SEND))  ? 'S' : '-';
	str[1] = (flags & (1 << SENDA)) ? 'A' : '-';
	str[2] = (flags & (1 << RECEIVE))  ? 'R' : '-';
	str[3] = (flags & (1 << SENDREC))  ? 'B' : '-';
	str[4] = (flags & (1 << NOTIFY)) ? 'N' : '-';
	str[5] = '\0';

	return str;
}
```

**逐行讲解**:
- `static char *s_traps_str(int flags)`：将陷阱掩码转换为字符串
  - S：SEND
  - A：SENDA（异步发送）
  - R：RECEIVE
  - B：SENDREC（发送并接收）
  - N：NOTIFY

---

## privileges_dmp函数

```c
/*===========================================================================*
 *				privileges_dmp 				     *
 *===========================================================================*/
void
privileges_dmp(void)
{
  register struct proc *rp;
  static struct proc *oldrp = BEG_PROC_ADDR;
  register struct priv *sp;
  int r, i;

  /* First obtain a fresh copy of the current process and system table. */
  if ((r = sys_getprivtab(priv)) != OK) {
      printf("IS: warning: couldn't get copy of system privileges table: %d\n", r);
      return;
  }
  if ((r = sys_getproctab(proc)) != OK) {
      printf("IS: warning: couldn't get copy of process table: %d\n", r);
      return;
  }
```

**逐行讲解**:
- `void privileges_dmp(void)`：显示进程权限表
  - 按F4触发
- `sys_getprivtab(priv)`：获取权限表
- `sys_getproctab(proc)`：获取进程表

---

```c
  printf("-nr- -id- -name-- -flags- traps grants -ipc_to--"
    "          -kernel calls-\n");

  PROCLOOP(rp, oldrp)
        r = -1;
        for (sp = &priv[0]; sp < &priv[NR_SYS_PROCS]; sp++)
            if (sp->s_proc_nr == rp->p_nr) { r ++; break; }
        if (r == -1 && !isemptyp(rp)) {
	    sp = &priv[USER_PRIV_ID];
        }
	printf("(%02u) %-7.7s %s %s %6d",
	       sp->s_id, rp->p_name,
	       s_flags_str(sp->s_flags), s_traps_str(sp->s_trap_mask),
		sp->s_grant_entries);
        for (i=0; i < NR_SYS_PROCS; i += BITCHUNK_BITS) {
	    printf(" %08x", get_sys_bits(sp->s_ipc_to, i));
       	}

	printf(" ");
        for (i=0; i < NR_SYS_CALLS; i += BITCHUNK_BITS) {
	    printf(" %08x", sp->s_k_call_mask[i/BITCHUNK_BITS]);
       	}
	printf("\n");

  }
}
```

**逐行讲解**:
- 使用`PROCLOOP`宏遍历进程表
- 查找每个进程对应的权限结构
- 打印权限ID、标志、陷阱掩码、授权条目数、IPC目标、系统调用掩码

---

## p_rts_flags_str函数

```c
/*===========================================================================*
 *			       p_rts_flags_str 				     *
 *===========================================================================*/
static char *p_rts_flags_str(int flags)
{
	static char str[10];
	str[0] = (flags & RTS_PROC_STOP) ? 's' : '-';
	str[1] = (flags & RTS_SENDING)  ? 'S' : '-';
	str[2] = (flags & RTS_RECEIVING)    ? 'R' : '-';
	str[3] = (flags & RTS_SIGNALED)    ? 'I' : '-';
	str[4] = (flags & RTS_SIG_PENDING)    ? 'P' : '-';
	str[5] = (flags & RTS_P_STOP)    ? 'T' : '-';
	str[6] = (flags & RTS_NO_PRIV) ? 'p' : '-';
	str[7] = '\0';

	return str;
}
```

**逐行讲解**:
- `static char *p_rts_flags_str(int flags)`：将运行时状态标志转换为字符串
  - s：进程停止
  - S：正在发送
  - R：正在接收
  - I：已发信号
  - P：信号待处理
  - T：ptrace停止
  - p：无权限

---

## proctab_dmp函数

```c
/*===========================================================================*
 *				proctab_dmp    				     *
 *===========================================================================*/
#if defined(__i386__)
void proctab_dmp(void)
{
/* Proc table dump */

  register struct proc *rp;
  static struct proc *oldrp = BEG_PROC_ADDR;
  int r;

  /* First obtain a fresh copy of the current process table. */
  if ((r = sys_getproctab(proc)) != OK) {
      printf("IS: warning: couldn't get copy of process table: %d\n", r);
      return;
  }

  printf("\n-nr-----gen---endpoint-name--- -prior-quant- -user----sys-rtsflags-from/to-\n");

  PROCLOOP(rp, oldrp)
	printf(" %5d %10d ", _ENDPOINT_G(rp->p_endpoint), rp->p_endpoint);
	printf("%-8.8s %5u %5u %6u %6u ",
	       rp->p_name,
	       rp->p_priority,
	       rp->p_quantum_size_ms,
	       rp->p_user_time, rp->p_sys_time);
	PRINTRTS(rp);
	printf("\n");
  }
}
#endif				/* defined(__i386__) */
```

**逐行讲解**:
- `void proctab_dmp(void)`：显示内核进程表
  - 按F1触发
- 条件编译：仅适用于i386架构
- 打印每个进程的信息：
  - 进程号、端点、名称
  - 优先级、时间片
  - 用户态时间、内核态时间
  - 运行时状态标志

---

## procstack_dmp函数

```c
/*===========================================================================*
 *				procstack_dmp  				     *
 *===========================================================================*/
void
procstack_dmp(void)
{
/* Proc table dump, with stack */

  register struct proc *rp;
  static struct proc *oldrp = BEG_PROC_ADDR;
  int r;

  /* First obtain a fresh copy of the current process table. */
  if ((r = sys_getproctab(proc)) != OK) {
      printf("IS: warning: couldn't get copy of process table: %d\n", r);
      return;
  }

  printf("\n-nr-rts flags--      --stack--\n");

  PROCLOOP(rp, oldrp)
	PRINTRTS(rp);
	printf("\n"); pagelines++;
	sys_diagctl_stacktrace(rp->p_endpoint);
  }
}
```

**逐行讲解**:
- `void procstack_dmp(void)`：显示进程栈跟踪
  - 按Shift+F9触发
- `sys_diagctl_stacktrace(rp->p_endpoint)`：打印进程栈跟踪

---

## proc_name函数

```c
/*===========================================================================*
 *				proc_name    				     *
 *===========================================================================*/
static char *
proc_name(int proc_nr)
{
  struct proc *p;
  if (proc_nr == ANY) return "ANY";
  if (proc_nr == NONE) return "NONE";	/* bogus */
  if (proc_nr < -NR_TASKS || proc_nr >= NR_PROCS) return "BOGUS";
  p = proc_addr(proc_nr);
  if (isemptyp(p)) return "EMPTY";	/* bogus */
  return p->p_name;
}
```

**逐行讲解**:
- `static char *proc_name(int proc_nr)`：根据进程号获取进程名
- 处理特殊值：ANY、NONE
- 检查边界
- 返回进程名称

---

## 要点总结

1. **数据获取**: 通过系统调用从内核获取数据副本，不直接访问内核内存。

2. **分页显示**: 使用`PROCLOOP`宏实现分页，防止输出过多信息。

3. **标志转换**: 将位标志转换为可读字符串，便于理解。

---

## 灾难预演

**如果删除`sys_getproctab`调用会怎样？**

进程表副本包含未初始化的数据。打印的信息是垃圾值，可能导致：
1. 进程名显示乱码
2. 端点号显示错误
3. 可能访问无效内存地址

**如果环形缓冲区处理错误会怎样？**

内核消息可能丢失或重复显示。如果`start`计算错误，可能：
1. 从错误位置开始读取
2. 缓冲区越界访问
3. 系统崩溃

---

## 互动自测

1. **内存模型**: `proc`数组的大小是多少字节？假设`NR_TASKS=16`，`NR_PROCS=64`，`struct proc`大小为1KB。

2. **设计选择**: 为什么使用静态缓冲区而不是动态分配？

3. **标志转换**: 如果进程的`p_rts_flags`为`RTS_SENDING | RTS_SIGNALED`，输出字符串是什么？
